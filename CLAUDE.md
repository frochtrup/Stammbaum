# Stammbaum — Claude-Anweisungen (v9)

Spezifikationsgetriebener Neuaufsatz. Der v8-Stand (Code in diesem Repo) ist **eingefrorene Referenz + Verhaltens-Orakel** — er wird nicht mehr weiterentwickelt. Der eigentliche v9-Bau zieht in ein eigenes lokales Repo um (siehe `specs/v9/31-Dev-Umgebung.md`), **nicht** in iCloud (INV-DEV-1).

## Aktiver Branch

`v9-dev`. Auf `v9-dev` committen; nach `main` nur per PR mit grüner CI.
*(v8-Branches `main`/`v8-dev` behalten ihre eigenen, v8-spezifischen Regeln — dieser Datei-Stand gilt nur auf `v9-dev`.)*

## Spezifikation = Quelle der Wahrheit

Das v9-System ist vollständig in `specs/v9/` spezifiziert. **Einstieg: `specs/v9/00-Index.md`.**

- **Vor jeder Änderung** das betroffene Subsystem-Spec lesen, dann handeln.
- Code widerspricht Spec → die **Spec gewinnt** (oder die Spec bewusst zuerst ändern).
- Entscheidungen von Tragweite → im **Entscheidungslog `specs/v9/04-Entscheidungslog.md`** festhalten (Skill `decision-log`).

## Skills (`.claude/skills/`, greifen automatisch)

- `altlast-audit` — v8-Bereich am echten Code prüfen → v9-Modell planen → Altlast §N
- `spec-new` — neues Subsystem-Spec anlegen + vollständig ins Set verdrahten
- `spec-lint` — Konsistenz des Spec-Sets prüfen
- `roundtrip-verify` — GEDCOM/GRAMPS-Roundtrip (`net_delta=0`) headless
- `decision-log` — Entscheidung als ADR-Eintrag festhalten

## Kern-Disziplin (nicht verhandelbar)

- **Am echten Code verifizieren, nicht aus Memory/Doku zitieren** — Docs driften (belegt).
- **Roundtrip-Treue (LP-1):** nach jeder Parser-/Writer-/Serializer-Änderung `roundtrip-verify`; `net_delta≠0` **nie** per Goldfile-Update übertünchen.
- **Vereinfachen vor Erfinden:** zuerst fragen, was den Zweck mit *weniger* Mechanismus löst.
- **Schicht-Invarianten ([02](specs/v9/02-Zielarchitektur-v9.md)):** INV-ARCH-1 (Abhängigkeiten nur nach unten; Kern DOM-/Framework-frei), INV-ARCH-2 (Kern build-frei testbar).
- **Erst OB delegieren, dann WIE (die folgenden Agenten-Regeln gelten nur, wenn diese hier für „ja" entschieden hat).** Ein Agent startet kalt: er liest Specs, exploriert Code und baut Kontext neu auf, den die Session bereits hat — das ist der Preis, nicht der Bau selbst. **Delegiere nur bei (a) echter Parallelität mit disjunkten Dateien oder (b) großer Exploration in unbekanntem Code.** Sind die betroffenen Dateien in dieser Session schon gelesen und die Änderung überschaubar: selbst bauen. **Belege (2026-07-16, ADR-v9-78 Punkte 3/5/6, s. [04a](specs/v9/04a-Chronik.md#adr-v9-78)):** Punkt 3 (~50 Zeilen, Dateien bereits gelesen) kostete als Agenten-Auftrag 190k Tokens plus eigene Nachprüfung — selbst gebaut ~30k; drei Agenten zusammen ~790k. Und (a) trügt leicht: Punkt 3 und Punkt 5 lagen beide in `PlaceDetail.svelte`/`place-detail-model.ts` — die Parallelität war Illusion, sichtbar erst beim Commit (keine saubere Trennung mehr möglich). **Vor dem `Agent`-Aufruf die Dateiliste beider Aufträge vergleichen, nicht die Themen** — zwei Themen sind kein Beleg für zwei Dateimengen.
- **Delegierte UI-/Insel-Bauabschnitte selbst gegenprüfen:** nach jeder Agenten-Delegation mit sichtbarer UI-Änderung eine eigene, unabhängige Browser-Verifikation (Screenshot/Snapshot/Inspect) fahren, bevor die Aufgabe als abgeschlossen gemeldet wird — der Agentenbericht allein reicht nicht (Lehre 2026-07-05: doppelte Kopfzeile + inkonsistenter Segment-Stil wurden vom Nutzer entdeckt, nicht vom Bauagenten selbst oder von mir vorab). **Ergänzung (2026-07-14, zwei parallele Agenten an einer gemeinsamen Schnittstelle, jeder für sich grün):** ein Agent, der die eine Hälfte einer neuen Schnittstelle baut (hier: `CoordIndicator` setzt einen Karte-Fokus-Slot), und ein zweiter, der die konsumierende Hälfte baut (Karte-Insel liest den Slot), können BEIDE unabhängig voneinander erfolgreich mit der ECHTEN Geschwister-Komponente verifizieren (kein Mock, kein Simulations-Artefakt) und trotzdem eine Lücke übersehen, wenn beide zufällig dasselbe „günstige" Testbeispiel wählen. Konkret: beide testeten mit einem bereits kuratierten Ort (eigene Koordinaten) — der tatsächlich häufigere, unangereicherte Fall (Koordinaten nur als Ereignis-Fallback, kein eigenes Orts-Datenfeld, ADR-v9-28/44 zufolge der Regelfall direkt nach Import) blieb ungetestet und war kaputt (Klick navigierte, zentrierte aber nichts — 0 gefundene Marker, reproduzierbar an echten Daten). Erst die eigene Nachverifikation mit einem GEZIELT unangereicherten Beispiel deckte es auf. Jetzt als benannter Kontrakt verankert ([32 TST-16](specs/v9/32-Testframework.md), referenziert aus `ui-builder.md`/`islands-builder.md`) — bei jeder Orts-/Hof-bezogenen eigenen Verifikation BEWUSST ein unangereichertes Objekt statt nur eines naheliegenden kuratierten Beispiels wählen.
- **Eigene Verifikation mit frischem Server, nach jedem Agenten-Lauf:** einen bereits laufenden, wiederverwendeten Preview-Server vor der eigenen Prüfung stoppen und neu starten — ein lange laufender/von mehreren Agenten geteilter Server kann HMR-Leichen zurücklassen, die zu falschen Navigations-/Zustandsfehlern führen (Lehre 2026-07-06: zwei parallel arbeitende UI-Agenten am selben Browser-Tab verursachten Race-Conditions, die erst mit einem frischen Server verschwanden). Bei MEHREREN parallelen UI-Agenten-Aufträgen (nicht nur zwei): möglichst unterschiedliche Dateien zuweisen, nicht denselben View gleichzeitig anfassen lassen. **Ergänzung (2026-07-10, 3 parallele Agenten):** eine reine Datei-Partitionierung schließt eine gemeinsame, neu zu extrahierende Shared-Komponente (INV-UI-4: „ein Mechanismus, nicht pro View neu erfunden") NICHT sauber aus — zwei der drei Agenten brauchten unabhängig voneinander dieselbe neue Gruppierungs-Logik; nur weil der zuerst fertige Agent sie zufällig bereits als eigene Datei extrahiert hatte und der zweite sie korrekt nur lesend importierte (nicht änderte/committete), blieb es folgenlos — kein verlässlicher Schutz, sondern gute Einzel-Entscheidung des Agenten. Bei künftigen parallelen Bau-Aufträgen, die laut Spec einen gemeinsamen Mechanismus verlangen: entweder (a) die geteilte Primitive selbst VORAB anlegen, bevor die Agenten gestartet werden, oder (b) im Prompt jedes Agenten explizit benennen, welche Datei(en) ein Geschwister-Agent möglicherweise neu anlegt, und wie damit umzugehen ist (lesend importieren erlaubt, nicht anfassen/committen).
- **Task-Notification ohne Abschlussbericht → sofort Repo-Zustand prüfen, nicht als „nichts passiert" werten:** trifft ein delegierter Agent das Ausgaben-Limit, enthält die Benachrichtigung nur eine Fehlermeldung statt der gewohnten Zusammenfassung — der Agent kann trotzdem bereits einen erheblichen, ggf. kaputten Zwischenstand hinterlassen haben (unfertige Verdrahtung, fehlende Tests, Referenzfehler). `git status`/`npm test` sofort prüfen; kleine Lücken selbst schließen statt einen weiteren Agenten mit demselben unvollständigen Kontext zu beauftragen (Lehre 2026-07-06/07). **Gilt auch bei mehreren PARALLELEN Agenten** — beide können unabhängig voneinander dasselbe Limit treffen (Lehre 2026-07-07, ADR-v9-37); die fehlende Lücke kann dabei größer sein als „unfertig" — bis hin zu **komplett fehlenden Tests** für einen sonst funktionierenden, kompilierenden Feature-Teil (in diesem Fall: selbst nachholen, test-first, bevor die Aufgabe als fertig gilt).
- **Oracle vs. Spec ist ein zweischneidiges Schwert ([32 TST-6](specs/v9/32-Testframework.md)):** weder das gesamte v8-Verhalten eines Feature-Bereichs blind nachbauen (reproduziert die evolutionär gewachsene Form, die der Neuaufsatz auflösen soll) noch alles im Spec-Bullet nicht explizit Genannte automatisch als außer Scope behandeln (verliert genealogisch relevante Information still, s. ADR-v9-23/24/27). Diskrepanzen zwischen knappem Spec-Text und reichhaltigem Oracle-Feature explizit benennen, nicht in eine Richtung auflösen. **Gilt bis auf Wert-Ebene:** nennt ein Spec-Bullet nur Tag-/Feldnamen ohne exakte Kodierung (z. B. Statuswerte, Datumstag-Wahl), ist das Spec-Bullet allein nicht genug — den echten v8-Quellcode nach der Byte-Struktur fragen, bevor eine eigene, plausibel wirkende Kodierung erfunden wird (Lehre 2026-07-07: `_TASK`-Writer wich beim ersten Bau vom v8-Format ab, weil nur Tag-Namen, nicht Werte, aus der Spec übernommen wurden — kein Test brach, weil der Tag neu war; nur eine Nutzerfrage nach evtl. vorhandenen echten Dateien deckte es auf).
- **Ein Fix an einer Regel ist erst fertig, wenn ALLE strukturgleichen Geschwister-Stellen mitgezogen sind — die gemeldete Stelle ist selten die einzige.** Nach jedem Fix an einer Invariante/Regel gezielt per `grep` die übrigen Anwendungsstellen suchen („wo gilt dieselbe Regel noch?"), statt nur den auffälligen Fall zu schließen. **Drei Belege aus EINER Sitzung (2026-07-16, Details in [04a](specs/v9/04a-Chronik.md)):** (a) ADR-v9-71 baute die Namensmengen-Prüfung in `seed.ts` nur in Pfad (a) ein (kuratierte POs) und übersah Pfad (b) (frisch geseedete Cluster) — der Doc-Kommentar über (a) beschrieb den Fall sogar wörtlich; Folge: vier Ortspaare doppelt, 23 Ereignisse unbindbar (ADR-v9-85). (b) `EventsByType.resetKey` war optional: `SourceDetail` setzte ihn, `PlaceDetail`/`HofList` vergaßen ihn — Zustands-Leck erst sichtbar, als eine zweite Änderung Ort→Ort-Navigation ohne Unmount ermöglichte. (c) `tsc --noEmit` prüfte `.ts`, Svelte-Templates gar nicht — vier echte Bugs unentdeckt (ADR-v9-83). **Wo mechanisch möglich, den Zwang statt die Erinnerung wählen:** `resetKey` wurde von optional auf Pflicht gehoben, damit der Compiler die Frage an JEDEM Aufrufer stellt (das deckte `HofList` auf) — das ist der Königsweg gegenüber jeder Dokumentation. Verwandt, aber enger gefasst: der Projektions-Hälften-Bullet unten (Nutzer-Edits an gecachten Feldern) und der Zwei-Agenten-Bullet oben — dies hier ist die allgemeine Form.
- **Bricht eine neue Funktion einen `INV-…`-Test, ist das zuerst ein Verdacht gegen die Funktion — nicht gegen den Test.** Ein Invarianten-Test hält eine bewusst getroffene Entscheidung fest; dass eine neue Anforderung an ihm scheitert, ist Information ÜBER die Anforderung. Die Testfassung „nachzuziehen", damit das Feature grün wird, verkehrt die Richtung: die Invariante war das Ergebnis einer Abwägung, das Feature ist noch keine. **Lehre (2026-07-19, ADR-v9-102):** die erste Umsetzung einer Personen-Vorbelegung koppelte `person` → `lensFocus` in `setCurrent` und brach zwei Tests, die die Unabhängigkeit der ViewState-Slots ausdrücklich festhalten (INV-VS, `tests/ui/view-state.test.ts`: „Ziele sind unabhängig voneinander (kein gemeinsamer currentX-Topf wie in v8)"). Mein erster Gedanke galt der Testanpassung; der Nutzer widerrief die Anforderung unabhängig davon einen Zug später und benannte das eigentliche Problem präziser. **Regel:** Anforderung gegen die Invariante halten und dem Nutzer vorlegen, statt zu glätten — eine Testanpassung ist erst zulässig, nachdem die Invariante selbst bewusst geändert wurde (dann mit ADR). Spiegelbild zum Ausnahme-Bullet weiter unten: dort wurde eine Regel zu weit außer Kraft gesetzt, hier sollte sie für einen Einzelfall aufgeweicht werden.
- **Eine einmal gefundene, in Code-Kommentaren dokumentierte Lehre kann unbemerkt wiederkehren — wo mechanisch möglich, strukturell erzwingen statt nur dokumentieren.** Eine Konvention, die nur in einer Agenten-Datei/einem Kommentar steht, hängt vom zufälligen Wieder-Erinnern jeder künftigen Bau-Session ab; ein Lint-/Architektur-Gate wirkt automatisch bei jedem `npm run lint`/CI-Lauf, unabhängig davon, ob der jeweilige Agent die Doku gelesen hat. **Lehre (2026-07-07):** die `<select bind:value>`-happy-dom-Falle wurde bereits einmal projektweit gefunden und behoben (in Kommentaren dokumentiert), tauchte aber in der nächsten größeren UI-Bau-Session in 7 neuen Stellen wieder auf — erst danach als ESLint-Regel (`no-restricted-syntax`, [32 TST-12](specs/v9/32-Testframework.md)) verankert, die den Fehler jetzt automatisch verhindert statt nur zu hoffen, dass er erinnert wird. Vor dem Anlegen eines neuen benannten Test-Kontrakts (TST-N) kurz prüfen, ob die Lehre stattdessen (auch) als Lint-Regel/Architektur-Check durchsetzbar ist — das ist strenger als jede Dokumentation.
- **Vor jeder neuen Einschränkung/Bestätigungspflicht erst prüfen, ob (a) eine bereits getroffene Entscheidung die Frage strukturell schon erzwingt, und (b) der Verzicht auf Automatisierung eine echte Korrektheits-Regression verursacht — nicht aus Prinzip eine neue, konservativere Regel erfinden.** Eine Sorge („X könnte mit Y konkurrieren/divergieren", „das braucht doch eine bewusste Nutzer-Bestätigung") rechtfertigt für sich allein noch keine Einschränkung. Zwei unabhängige Fundstellen, **derselbe Fehlschluss-Typ, zweimal aufgetreten** — deshalb bewusst allgemein formuliert, nicht subsystem-spezifisch: **Lehre (2026-07-08, ADR-v9-41→42):** ein Ereignis-Ort-/Adress-Picker wurde auf „nie eine ID setzen, nur Text vorschlagen" beschränkt, aus Sorge, ein UI-Kommando könnte mit `resolveEvents()` um die Zuordnungs-Wahrheit konkurrieren — dabei existierte `linkEventToPlace()` (Sofort-Reprojektion, ID + Text atomar) bereits seit ADR-v9-19 genau für diesen Fall. **Lehre (2026-07-10, ADR-v9-45→Nachtrag/ADR-v9-48):** ein automatischer Hof-Nachlauf nach Dorf-Merge wurde zunächst abgelehnt, weil ein Kaskaden-Merge „eine bewusste Nutzer-Bestätigung" brauche — dabei war (a) `(villageId, normalisierte Adresse)` bereits die strukturelle Hof-Identität, der Dorf-Merge erzwingt die Konsolidierung also bereits, und (b) der Verzicht keine bloße Unordnung, sondern eine echte Resolver-Regression (`findByAddr` liefert bei ≥2 Kandidaten `null`, vormals eindeutige Events kippen in Review). Beide Male deckte erst eine Nutzerrückfrage es auf, nicht eigene Vorabprüfung. **Vor einer neuen Einschränkung:** gezielt (a) nach vorhandenen `link*`-/Sofort-Reprojektions-Kommandos oder sonstigen bereits getroffenen Entscheidungen suchen, die dieselbe Frage schon strukturell beantworten, und (b) am abhängigen Code verifizieren, ob der Verzicht auf Automatisierung eine echte Korrektheits-Regression erzeugt (nicht nur nach der zugrunde liegenden Invariante fragen, sondern das tatsächliche Verhalten einer betroffenen Lookup-/Match-Funktion bei Mehrdeutigkeit prüfen). **Spiegelbild-Fall (2026-07-19, BL-06→BL-96): eine selbst geschriebene AUSNAHME ist derselbe Fehlschluss in die andere Richtung.** Beim Abschluss der Desktop-Sidebar hielt ich im Spec fest, das Befehlsflächen-Budget (INV-UI-11) sei „mobil skopiert" und gelte oberhalb 900px nicht — Begründung: „dort trägt die Sidebar die Beschriftungen". Die Begründung stimmte, aber nur für NAVIGATION; zwei Bauabschnitte später ergab die Messung, dass die Desktop-Listenspalte 352px misst und damit schmaler ist als die 375px, für die das Budget überhaupt formuliert war. Der Fehler war nicht die Zahl, sondern die Reichweite: eine Begründung, die einen Teil der Flächen trägt, wurde auf alle ausgedehnt. **Wer eine Regel für einen Bereich außer Kraft setzt, prüft die Begründung gegen JEDE Art von Fläche in diesem Bereich — nicht nur gegen die, die den Gedanken ausgelöst hat.** Und: eine Ausnahme, die sich in einer Zahl ausdrücken lässt, wird gemessen, bevor sie ins Spec geschrieben wird, nicht danach.
- **Kurations-/Wildwuchs-Sorgen (ADR-024-Familie) gelten für automatische Massenanlage beim Import, nicht für einzelne, geprüfte Nutzerentscheidungen im Editier-Modus — diese zwei werden leicht verwechselt.** **Lehre (2026-07-08, ADR-v9-40→42):** Ort/Hof wurden zunächst pauschal von jeder Picker-„+ neu anlegen"-Option ausgeschlossen, mit Verweis auf genau diese Kurations-Sorge — dabei betraf die Sorge ursprünglich nur unkontrollierten Import-Wildwuchs, nicht eine einzelne, durch vorherige Suche bereits geprüfte Anlage-Handlung. Jetzt in [11 §2](specs/v9/11-Orte-Hoefe-Identitaet.md) explizit als Unterscheidung verankert (Import-Zeit vs. UI-Zeit) — bei künftigen Ort/Hof-Entscheidungen dort zuerst nachlesen, bevor dieselbe Verwechslung ein drittes Mal passiert.
- **Ein Nutzer-Edit an einem projizierten/gecachten Feld ist erst fertig, wenn ALLE gekoppelten Repräsentationen mitgezogen sind — eine „live berechnete" und eine „eingefrorene" Hälfte derselben Sache ist ein still halb-erledigter Edit.** Und: eine Freeze-/Roundtrip-Invariante, die UNGEWOLLTE AUTOMATISCHE Änderungen verhindern soll, darf NICHT reflexartig auf EXPLIZITE Nutzer-Edits angewandt werden — das verkehrt ihren Zweck. **Lehre (2026-07-14, ADR-v9-81):** die Hof-Umbenennung wurde zuerst „gebaut" (`withUpdatedHofAddr` + inline-editierbare Adresszeile), von mir selbst live verifiziert (Steckbrief/Liste/Titel aktualisierten sich) und als fertig gemeldet — dabei war sie strukturell halb: der Writer berechnet `PLAC` live aus den Hof-`addrs`, aber `ADDR` wird aus dem eingefrorenen `event.addr` geschrieben (ADR-v9-47 Fill-if-empty), und die Ereigniszeile zeigt `event.addr` roh. Folge: die Umbenennung war auf JEDER referenzierenden Ereigniszeile unsichtbar, der Export erzeugte inkonsistente PLAC/ADDR, und der nächste Lade-Pass re-bootstrappte den alten Namen (weil `event.hofId` runtime-only ist und aus dem — alten — ADDR neu aufgelöst wird). Erst die fachliche Nutzer-Rückfrage („der neue Name soll doch durchgängig verwendet werden … die referenzierenden Adresseinträge müssen angepasst werden") deckte es auf, nicht meine eigene Verifikation — weil ich nur die drei naheliegenden Anzeigen prüfte (Steckbrief/Liste/Titel), nicht die referenzierende Ereigniszeile UND nicht den Reload-Persistenz-Pfad. **Zwei konkrete Reflexe für künftige Edits an einem Feld, das anderswo als Cache/Projektion gespiegelt wird:** (a) den Schreib-/Anzeige-Pfad JEDER Spiegelung auflisten (hier: PLAC-Writer live, ADDR-Writer eingefroren, Ereigniszeile roh, Reload-Resolver aus ADDR) und für jede prüfen, ob der Edit ankommt — die eigene Verifikation MUSS eine referenzierende Fremd-Ansicht UND einen Reload umfassen, nicht nur die Objekt-eigenen Ansichten (TST-8/TST-14-Geist); (b) stößt der Edit auf eine Invariante wie ADR-v9-47, die Änderungen „einfriert", zuerst fragen, ob diese Invariante AUTOMATISCHE oder EXPLIZITE Änderungen meint — ein Freeze gegen Auto-Reprojektion ist kein Freeze gegen bewusste Nutzer-Edits (LP-1 wird stattdessen mit einem gezielten Guard gewahrt: nur „saubere" Cache-Werte mitziehen, byte-abweichende quellen-eigene Werte unangetastet lassen). **Ergänzung (2026-07-19, ADR-v9-102) — die Stufe davor: ein erhaltener Zustand, den ein ZWEITER, unabhängiger Zustand verdeckt, ist aus Nutzersicht nicht erhalten.** Die Karte kehrte nach dem Wegnavigieren korrekt mit ihrer Personenauswahl zurück — aber im Modus „Orte“, der diese Auswahl gar nicht anzeigt; zwei getrennt gebaute Hälften, jede für sich richtig, zusammen wirkungslos. Aufgefallen nur, weil die eigene Verifikation den ganzen Weg ging (auswählen → wegnavigieren → zurück → **hinsehen**) statt den Zustand per Abfrage zu prüfen. **Prüfform:** „ist es nach der Rückkehr SICHTBAR?“, nicht „ist es gespeichert?“.
- **Bei aktiv im Gespräch weiterentwickeltem Design: Bauagenten erst NACH dem Einpendeln des Designs starten, nicht beim ersten plausiblen Stand.** Ein bereits laufender Agent, dessen Auftrag durch eine Nutzer-Rückfrage mitten in der Umsetzung überholt wird, muss redirected/gestoppt werden — Kontext-Overhead und ggf. verworfene Arbeit. **Lehre (2026-07-08):** ein Bauagent für „Text-Autocomplete ohne ID" (ADR-v9-41) wurde gestartet, bevor die anschließende Nutzer-Diskussion die Richtung auf „Picker mit Sofort-Reprojektion" (ADR-v9-42) drehte — der Agent hatte glücklicherweise noch keine Datei angefasst (per Rückfrage bestätigt, kein Schaden), aber das war Zufall, kein verlässlicher Schutz. Bei spürbar noch offener Architekturfrage im Dialog: mit der Delegation warten, bis der Nutzer eine Richtung bestätigt hat.
- **Bei Agenten-Delegation in ein ANDERES Repo als das Session-Projekt (hier: Bau-Agenten arbeiten immer in `~/dev/stammbaum-v9`, nie im Spec-Repo, INV-DEV-1) `isolation: worktree` NICHT setzen, und nach Abschluss immer per `git status`/`git branch --show-current` im echten Zielverzeichnis verifizieren.** Worktree-Isolation eines Tools bezieht sich vermutlich auf das Session-Projekt-Repo, nicht auf einen im Prompt genannten, abweichenden absoluten Pfad — das kann einen Agenten in einer isolierten Kopie des FALSCHEN Repos arbeiten lassen, ohne dass es aus dem Bericht ersichtlich wäre. **Lehre (2026-07-10, ADR-v9-48):** der erste Bau-Agent des Orte/Höfe-Kurationskomplexes bekam `isolation: "worktree"`, obwohl er explizit im separaten Code-Repo arbeiten sollte — folgenlos geblieben, aber nur weil nachträglich per `git status` im echten Zielverzeichnis gegengeprüft wurde, nicht weil die Wahl selbst abgesichert war. Für Folge-Agenten in dieselbe Situation `isolation` weglassen und die Verifikation zur festen Gewohnheit machen, nicht zur Ausnahme. **Ergänzung (2026-07-11) — die Kehrseite des Weglassens von `isolation`:** ohne Isolation arbeitet der Bau-Agent im GENAU SELBEN Git-Arbeitsverzeichnis wie die eigene Session — das schließt die Repo-Verwechslung von oben aus, öffnet aber ein neues Risiko: eigene DIREKTE Bearbeitung (Bash/Edit/Commit) IM SELBEN Verzeichnis, während der Agent noch läuft, kann mit seinem Arbeitszustand kollidieren. **Lehre (2026-07-11):** während ein `ui-builder`-Agent im Hintergrund am Code-Repo arbeitete (Event-Edit-Modal-Auftrag, kein `isolation` gesetzt, wie oben vorgeschrieben), committete und pushte ich in derselben Sitzung einen eigenen, unabhängigen Fix (Ereigniszeilen-Sortierung) direkt in dasselbe Arbeitsverzeichnis — der Agent wurde danach ohne Bericht abgebrochen ("Agent was stopped", vom Nutzer NICHT ausgelöst, per Rückfrage bestätigt). Ursache nicht zweifelsfrei belegt (kein Einblick in die interne Abbruch-Logik des Orchestrierungstools), aber der zeitliche Zusammenhang ist der einzige plausible Auslöser — kein anderer Eingriff fand im fraglichen Zeitraum statt. **Regel:** solange ein Bau-Agent ohne `isolation` im Code-Repo läuft, KEINE eigenen direkten Bearbeitungen/Commits/Pushes im SELBEN Repo vornehmen, auch nicht für einen scheinbar unabhängigen Klein-Fix in anderen Dateien — entweder auf den Agenten-Abschluss warten (Benachrichtigung kommt automatisch), oder eigene dringende Arbeit im Spec-Repo erledigen (dort keine Kollision möglich), oder den Bau-Agenten bewusst zuerst fertig laufen lassen, bevor ein weiterer, eigener Eingriff im selben Code-Repo startet. **Zweiter Vorfall trotz dieser bereits dokumentierten Regel (2026-07-12, ADR-v9-65):** ein Bau-Agent für die Item-Modal-Architektur bekam beim allerersten Aufruf dieser Sitzung `isolation: "worktree"` — exakt der oben beschriebene, bereits bekannte Fehler, trotz wörtlich vorliegender Regel im eigenen Kontext. Folgenlos geblieben NUR weil der Nutzer den Agenten selbst stoppte ("stopped it"), bevor Schaden entstehen konnte — nicht weil eine eigene Vorabprüfung es abgefangen hätte. Der zweite, korrekte Versuch (ohne `isolation`) gelang danach fehlerfrei. Es gibt für diese konkrete Fehlerklasse KEIN mechanisches Enforcement (ein Agent-Tool-Parameter zur Aufrufzeit hat keinen Lint-/CI-Ansatzpunkt) — die einzig verfügbare Verstärkung ist Wiederholung mit konkretem Beleg: **vor JEDEM `Agent`-Aufruf, dessen Prompt ein Arbeitsverzeichnis außerhalb des Session-Projekts nennt (hier: `~/dev/stammbaum-v9`), den `isolation`-Parameter als expliziten Checklistenpunkt behandeln — weglassen ist der Normalfall, nicht die Ausnahme, die man aktiv begründen muss.**
- **Eigene Browser-Verifikation: `form_input`/`computer{action:"left_click"}` (aktueller Tool-Namensraum `mcp__Claude_Browser__*`) verwenden, nicht manuelle `dispatchEvent()`-Konstruktionen, wenn ein Svelte-Feld über `onchange` (nicht `bind:value`) reagiert.** Ein per JS gesetzter Input-Wert + synthetisch dispatchtes `input`/`change`-Event löst bei `onchange`-gebundenen Feldern nicht zuverlässig dieselbe Reaktivität aus wie ein echter Blur nach echtem Tippen. **Lehre (2026-07-08):** ein Adress-Picker-„+ Hof anlegen"-Button schien den getippten Text nicht zu übernehmen (Fehlalarm) — lag an der eigenen Testmethode (`Object.getOwnPropertyDescriptor(...).set` + `dispatchEvent`), nicht am Code; mit dem dedizierten Fill-Tool reagierte dasselbe Feld sofort korrekt. Bei einem scheinbaren UI-Bug zuerst die eigene Testmethode gegenprüfen, bevor er als echter Befund gemeldet wird. **Ergänzung (2026-07-12):** der MCP-Tool-Namensraum selbst wurde zwischenzeitlich von `mcp__Claude_Preview__*` auf `mcp__Claude_Browser__*` umbenannt (teils mit anderer Methodenaufteilung, z. B. `preview_fill`→`form_input`, `preview_click`→`computer`) — `.claude/agents/ui-builder.md`/`islands-builder.md` referenzierten bis ADR-v9-65 noch den alten Namensraum, wodurch drei Bau-Agenten in Folge fälschlich „keine Preview-Tools verfügbar" meldeten. Bei einem ähnlichen Fund künftig: Tool-Namen in Agenten-`tools:`-Frontmatter sind eine potenzielle Drift-Quelle wie jede andere Doku auch, nicht nur der Fließtext.
- **Bei Häufigkeits-/Priorisierungsentscheidungen den echten, aktuell genutzten Datenbestand auszählen — aber die tatsächlich geladene Datei verifizieren, nicht die erstbeste Fixture im Repo.** Eine Auszählung gegen `tests/fixtures/*.ged` sieht plausibel aus, ist aber nicht automatisch derselbe Datenbestand, den der Nutzer tatsächlich in der laufenden App verwendet — mehrere ähnlich benannte `.ged`-Dateien mit unterschiedlicher Satzzahl können nebeneinander existieren. **Lehre (2026-07-11, ADR-v9-62/65):** eine Ereignis-Pill-Priorisierung wurde zunächst gegen `tests/fixtures/MeineDaten_ancestris.ged` (2795 Personen) ausgezählt — der Nutzer deckte per Screenshot der laufenden App-Statistik auf, dass die Zahlen nicht zusammenpassten (App zeigte Verhältnisse, die auf ~3120 Personen hindeuteten). Erst der Abgleich der Personenzahl gegen ein sichtbares App-Statistik-Widget führte zur richtigen Datei. **Vor einer Auszählung, deren Ergebnis in eine Spec-Entscheidung einfließt:** die Personen-/Familienzahl der herangezogenen Datei gegen eine im laufenden System sichtbare Zahl (Statistik-Ansicht, Listen-Zähler) gegenprüfen, nicht einfach die naheliegendste Datei nehmen.
- **„Grün" und „getan" sind nicht dasselbe wie „tut das, wozu es da ist" — ein neu gebautes Mess-/Wächter-Instrument ist erst fertig, wenn seine WIRKUNG geprüft ist, nicht sein Exit-Code.** Bei einem Feature prüft der Test das Verhalten; bei einem *Gate, Lint, Perf-Budget oder Backlog-Beleg* ist das Instrument selbst das Produkt — und ein grüner Lauf beweist von ihm nur, dass er nicht abgestürzt ist. **Drei Belege aus EINER Sitzung (2026-07-18, BL-47/48, ADR-v9-91):** (a) Der `test:perf`-Schritt wurde in CI verdrahtet und nur auf Exit-Code geprüft — Vitests Standard-Reporter unterdrückt `console.log` aus GRÜNEN Tests restlos (auch mit `CI=true`), das CI-Log hätte also nur „1 passed" enthalten und **genau die Messwerte nicht, für die das Gate existiert** („die Zahl ist der eigentliche Nutzen, die Schwelle nur der Wecker" — so der Kopfkommentar des Tests selbst). Aufgefallen erst durch die Nutzerfrage „wo finde ich diese Angabe?". (b) Das Budget dieses Gates war eine **Schätzung über fremde Hardware** („Runner 2–3× langsamer"), behandelt wie eine gemessene Zahl; die erste echte CI-Messung ergab 3–4,5× — und zeigte, dass nicht nur der Wert, sondern die **Bemessungsregel** falsch war (sie ankerte am Ist der Referenz-Hardware, obwohl das Gate auf dem Runner läuft). **Eine Schwelle für fremde Hardware lässt sich nicht auf eigener Hardware kalibrieren**; bis zur ersten Messung dort ist jede solche Zahl eine Schätzung und als solche zu kennzeichnen. (c) Ein Backlog-Eintrag (BL-79) wurde dem Nutzer **vorgeschlagen, bevor gemessen war** — die anschließende Messung drehte die Prämisse um (die vermutete Quadratik lag in der Hof-, nicht der Ereigniszahl, sättigend, Anteil SINKEND beim Skalieren), der Eintrag wurde am selben Tag zurückgezogen. **Drei Reflexe:** (1) Nach dem Verdrahten eines Gates/Lints dessen Ausgabe **dort lesen, wo sie konsumiert wird** (CI-Log, nicht die lokale Shell mit anderen Flags) — lokal mit `--reporter=verbose` zu messen und in CI ohne, ist zwei verschiedene Werkzeuge zu prüfen. (2) Den Wächter **negativ testen**: das Bewachte kaputtmachen und sehen, ob er anschlägt (bei BL-47/48 gemacht — `it.skip` zurückgesetzt bzw. CI-Zeile gelöscht, L2 schlug an); ein Wächter, dessen Rot-Fall nie gesehen wurde, ist unbelegt. (3) **Zahlen vor Zeilen:** keine Backlog-/Priorisierungs-Aussage auf eine Einschätzung hin, die in Minuten messbar wäre — verwandt mit dem Bullet darüber, aber eine Stufe früher: dort geht es um die *richtige* Messung, hier um *überhaupt* eine.
- **Nach einem skriptgesteuerten Edit über mehrere Dateien den Diff lesen, nicht nur den Testlauf.** Ein grüner Lauf beweist, dass das Verbliebene funktioniert — nicht, dass nichts verlorenging. **Zwei Schäden aus EINER Sitzung (2026-07-19, Details in [04a](specs/v9/04a-Chronik.md)):** (a) ein pauschales `replace` über mehrere Test-Dateien traf eine Zusicherung, die einen Listeneintrag meinte statt des Feldes — laut fehlgeschlagen, also harmlos; (b) ein Anhänge-Skript mit `s.rstrip()[:-len("")]` schnitt eine Testdatei auf **leer** (`[:-0]` ist `[:0]`) und ließ sie von 215 auf 36 Zeilen schrumpfen. Aufgefallen nur, weil der Folgefehler laut war; wäre ein kleinerer, aber lauffähiger Rest stehen geblieben, hätte der grüne Lauf den Verlust gedeckt. Die Wiederherstellung per `git checkout` kostete zusätzlich alle Sitzungsänderungen an der Datei. **Reflex:** `git diff --stat` nach jedem Massen-Edit, Zeilenbilanzen gegenlesen; bei Python-Slices mit berechneter Grenze die Kante `-0` bedenken.

## Tests

Kern-Tests headless & build-frei (Vitest, [32](specs/v9/32-Testframework.md)). **Jede `INV-…`/`LP-…` hat einen Test (TST-2).** Pre-Commit = schneller Kern-Subset; CI = alle Ebenen, Deploy nur bei grün.
*(Hinweis: der aktuelle Git-Pre-Commit-Hook läuft noch die v8-Tests — für v9-Doku-Commits unschädlich; wird beim Repo-Umzug durch das Vitest-/CI-Gate ersetzt.)*

## Stack (entschieden)

UI-Schale: **Svelte 5 + Vite** (kein SvelteKit); Domänenkern framework-frei ([02](specs/v9/02-Zielarchitektur-v9.md)). Tests: Vitest + `@testing-library/svelte` ([32](specs/v9/32-Testframework.md)). Einmalige Voraussetzung: Node.js. **Baubeginn kann starten.**

## Projektpfade

- **Spec-Repo (dieses Repo):** `/Users/franzdecker/Documents/GitHub/Stammbaum`.
- **v9-Code-Repo (Baufortschritt):** `/Users/franzdecker/dev/stammbaum-v9` (separates Git-Repo, `INV-DEV-1`/ADR-v9-08 — außerhalb iCloud). Bau-Agenten arbeiten dort.
- **v8-Original (eingefroren, nur Referenz):** `/Users/franzdecker/Library/Mobile Documents/com~apple~CloudDocs/Genealogie/AppDev/files/`.
