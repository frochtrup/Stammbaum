# Unabhängige Gesamtbewertung — Stammbaum PWA

*Stand: 2026-06-19 · Branch `v8-dev` · sw v998 · unabhängiges Review (Code gelesen, Tests selbst ausgeführt, Architektur-/Sicherheits-Claims gegengeprüft)*

> Diese Bewertung wurde unabhängig von der Selbstbewertung in `ROADMAP.md` erstellt. Wo sie abweicht, ist die Begründung genannt. Ziel: unvoreingenommene Standortbestimmung inkl. Hinterfragung vergangener Entscheidungen.

---

## Verifikationsbasis

Selbst nachgeprüft (nicht aus Doku übernommen):

- `test-unit.js` → **558/558 grün** ✓
- `test-csp.js` → grün, `index.html` frei von inline-`on*`/`style` ✓
- `test-snapshot-place.js` → 2 Goldfiles OK ✓
- `test-roundtrip.js MeineDaten_ancestris.ged` → **`net_delta=0`, stable, 331 ms**; Strict-Variante stable ✓
- Codebasis vermessen: 78 Quelldateien · ~42k JS-LOC · `gedcom.js` = 2.339 Z./96 Top-Level-Funktionen · 54 `<script>`-Tags · OAuth-/CSP-/XSS-Oberfläche manuell gelesen.

Befund: Die zentralen Versprechen halten der direkten Prüfung stand. Die ROADMAP-Noten sind größtenteils ehrlich, an einzelnen Stellen leicht großzügig (siehe unten).

---

## Bereichsbewertungen

### 1. Architektur — **6.5/10** (Selbst: 6.9)

**Stärken:** Saubere Schichtung (Parser/Writer ↔ State ↔ View); 26 ADRs dokumentieren das *Warum*; Passthrough-Fundament (ADR-012) ist die klügste Einzelentscheidung und macht `net_delta=0` erst möglich; `placeObjects` als single source of truth konzeptionell sauber.

**Schwächen:**
- `gedcom.js` ist ein **God-Module** (2.339 Z., 96 Funktionen) — größte strukturelle Schuld, und sie *wächst* (ADR-024/026 trugen kräftig bei).
- **54 Script-Tags mit impliziter Ladereihenfolge** (`ui-event-delegation.js` muss letztes Script sein) — versteckte Kopplung, hält nur durch Disziplin.
- **~860 Globals, `idbGet` von 17 Dateien referenziert.** Die ESM-Brücken-Migration (ADR-020) ist nach 2 von ~50 Dateien **faktisch stehengeblieben** — der inkrementelle Pfad greift empirisch nicht.

*Abweichung nach unten:* God-Module-Trend verschlimmert sich mit jedem Orts-Feature, statt sich zu stabilisieren.

### 2. Code-Qualität — **8.0/10** (Selbst: 8.0) ✓

Bestätigt nach Lesen von echtem Code: Beziehungsrechner (`calcRelationship`/`_relLabel`) vorbildlich (BFS mit Zyklus-Schutz, elegante dt. Verwandtschaftsterminologie). 1 echtes TODO + nur Fallback-`console.log` in 42k LOC. 754 `esc()` gegen 223 `innerHTML` — konsequente Disziplin. *Abzüge:* `showDetail` (277 Z.) + `_pdetLifeData` (201 Z.) als letzte Monsterfunktionen; nur 11/71 Dateien mit `'use strict'`.

### 3. Sicherheit — **8.5/10** (Selbst: 8.5) ✓

Unabhängig verifiziert: OAuth **lehrbuchkorrekt** (PKCE `S256` via echtem `crypto.subtle.digest`, `state`-CSRF-Schutz, kein `client_secret`, Token-Expiry+Refresh). `sessionStorage` für Tokens ist die richtige Wahl. CSP ohne `unsafe-inline`/`eval` = wirksamster XSS-Schutz. Keine externen Runtime-Deps (Leaflet 144K + Fonts lokal vendored) → Supply-Chain-Risiko ≈ null.

*Korrektur zur Doku:* Die „20 inline-styles in JS" liegen fast alle in `ui-print.js`, also in separaten Druck-HTML-Dokumenten (Blob-Fenster), die **nicht** der App-CSP unterliegen → kein Laufzeit-Risiko in der PWA; in der Schuld-Liste leicht überzeichnet.

### 4. Design / UX — **8.0/10** (Selbst: 8.5)

Design-Token-System, Mobile-First, 0 WCAG-2.1-AA-Violations (v905). *Abzug:* Handbuch teils Mockups statt echten Screenshots — gerade „visuell-einsteigerfreundlich" (erklärtes Kernziel) bleibt damit unbelegt.

### 5. Tests / Konformität / PWA — **Tests 8.7 · GEDCOM 9.5 · PWA 9.0** (≈ Selbst)

Reproduziert und verdient. `net_delta=0` + `out1===out2` auf 83k-Zeilen-Produktionsdatei = härtester Konformitätsbeweis im Genealogie-Bereich; GEDCOM eher unterbewertet. Einziger Abzug bei Tests: kein CI-Server (Hooks nur lokal).

### Weitere Bereiche (bestätigt)

Performance 8.0 · Datenschutz 9.0 · Funktionsstand 9.0 · Funktions-Qualität 8.5 — alle ≈ Selbstbewertung.

---

## Funktionsvergleich mit etablierten Programmen (unabhängig)

| Dimension | Stammbaum PWA | Einordnung |
|---|---|---|
| GEDCOM-Treue | **Klassenbester** | schlägt MacFamilyTree *und* Ancestry (beide verlustbehaftet), ebenbürtig GRAMPS — verifiziert |
| Historisch datierte Orte | **Einzigartig** | periodengerechter Picker + Verwaltungs-Zeitlinie in keinem Mainstream-Tool |
| Quellen/Forschungs-Workflow | Stark | besser als MFT, noch nicht GRAMPS-Niveau (Goldstandard Quellentiefe) |
| Reports | Sehr gut | 12 Formate top für Solo; MFT bei Buch-/Poster-Politur vorn |
| Plattform/Datenschutz/Kosten | **Konkurrenzlos** | gratis + überall offline + kein Tracking |
| Sync | Schwach | manueller OneDrive-Trigger vs. MFT CloudKit / Ancestry-Cloud — klarste reale Lücke |
| DNA / Online-Records | Fehlt | bewusst out-of-scope (kategoriefremd) |
| Mobile Feldarbeit | **Gut** | CAM (Kamera 📷/🖼, lokal ohne OneDrive) seit v59 vorhanden; Schnellzugriff v999. *(Review hatte dies anfangs falsch als Lücke geführt — korrigiert.)* |

**Einordnung:** Für lokal/DSGVO-orientierte Hobby-Genealogen funktional konkurrenzfähig zu kommerziellen 20-Jahres-Produkten, in GEDCOM-Treue + historischer Ortsdarstellung überlegen. Verliert nur dort, wo Geld/Cloud nötig ist (Sync, DNA, Online-Matching).

---

## Hinterfragung vergangener Entscheidungen

**① „Kein Build-Step" (ADR-001/002) — hält besser stand als zunächst behauptet.** *(revidiert 2026-06-19)*
Erst-Fassung: „Bundler-Trigger faktisch erreicht, esbuild würde God-Module + Ladereihenfolge + Globals gemeinsam entschärfen." **Beides falsch, nach Prüfung:**
- Der *Startup*-Teil des Triggers entfällt — Produktion läuft auf **HTTP/2** (GitHub Pages, `curl`-verifiziert: HTTP/2 + gzip + ETag). Die „57 Round-Trips" sind ein HTTP/1.1-Problem; auf HTTP/2 gemultiplext, gemessener Bundle-Gewinn = wenige ms. Kein User-Nutzen.
- Ein Bundler hätte das *God-Module/Globals*-Problem **nie** gelöst (Concatenation packt dieselben Globals nur in eine Datei). Architektur und Bundling sind getrennte Themen.
**Korrigierte Sicht:** ADR-001/002 ist *richtiger* als ich dachte — der no-Build-Ansatz kostet auf HTTP/2 nichts Spürbares. Die *reale* Architektur-Schuld (God-Module `gedcom.js`, ~860 Globals) bleibt — aber sie ist per **manuellem `<script>`-Splitting ohne Build-Step** lösbar (GEDCOM-SPLIT), genau im Geist von ADR-001/002. Der ESM-Vollumbau (Module-Worker, 17-Datei-`idbGet`-Kaskade) bleibt XL und trigger-basiert (ADR-020) — der Trigger ist **nicht** erreicht.

**② Riesiger Effort in Orts-Historie (ADR-024/026, >200 Versionen).**
Technisch exzellent und einzigartig — band aber über Monate den Großteil der Entwicklung an ein Nischen-Feature. *(Korrektur v999: Die ursprüngliche Formulierung „CAM steht bis heute im Backlog" war falsch — CAM ist seit v59 implementiert; der ROADMAP-Backlog-Eintrag war veraltet. Die Priorisierungs-Kritik entfällt damit; bleibt: die Orts-Historie ist ein Nischen-Differenzierer, dessen Aufwand-Nutzen ggü. breiteren Hebeln wie der Architektur-Schuld abzuwägen ist.)*

**③ Drei parallele Orts-Speicher.** Jahrelange Konsolidierung war nötig wegen einer selbstgeschaffenen Design-Schuld; gut behoben, aber `T0-EXTRAPLACES-CLEANUP` + `hofObjects`-Sidecar zeigen, dass sie noch nicht ganz abgeschlossen ist.

**④ Noten-Tendenz.** Selbst-∅ 8.6 ist *leicht* optimistisch — primär wegen Architektur (God-Module-Wachstum) und Design/UX (unbelegte Einsteigerfreundlichkeit). Die funktionalen Noten (Roundtrip/Tests/Konformität) sind dagegen voll verdient.

---

## Gesamttabelle

| Bereich | Selbst | **Review** | Abweichung |
|---|---|---|---|
| Architektur | 6.9 | **6.8** ↑ | God-Module wächst (Hauptschuld), aber no-Build (ADR-001/002) ist auf HTTP/2 **kostenfrei** und die Schuld in-paradigm per manuellem Split lösbar — kein Bundler nötig. Erst-Wert 6.5 war von der widerlegten „Bundler-Trigger erreicht"-Prämisse gedrückt. |
| Code-Qualität | 8.0 | **8.0** | bestätigt |
| Sicherheit | 8.5 | **8.5** | OAuth/CSP/PKCE verifiziert |
| Design / UX | 8.5 | **8.0** | Einsteigerfreundlichkeit unbelegt |
| Funktionsstand | 9.1 | **9.0** | breit & tief; CAM doch vorhanden (Korrektur) |
| Funktions-Qualität | 8.5 | **8.5** | empirisch top |
| Performance | 8.0 | **8.2** ↑ | Worker + VS + Sort-Cache; der „~50 Cold-Start-Requests"-Abzug entfällt — HTTP/2-Multiplexing gemessen (kein Round-Trip-Problem). Rest-Abzug: erster Sort 20k ~1 s, >20k unbelegt (SCALE-REAL). |
| GEDCOM-Konformität | 9.3 | **9.5** | selbst reproduziert — eher unterbewertet |
| Tests | 8.7 | **8.7** | bestätigt; CI fehlt |
| PWA / Offline | 9.0 | **9.0** | echt offline, lokal vendored |
| Datenschutz | 9.0 | **9.0** | konkurrenzlos |
| **∅ Gesamt** | **8.6** | **≈ 8.4** | zwei kleine Upticks (Architektur 6.5→6.8, Performance 8.0→8.2) nach HTTP/2-Messung; ∅ bleibt gerundet ≈ 8.4 |

**Fazit:** Außergewöhnlich diszipliniertes Solo-Projekt, dessen Kernversprechen der Prüfung standhalten und kommerzielle Produkte in den Kerndisziplinen schlagen. Die verbleibende reale Schwäche ist das wachsende God-Module `gedcom.js` — behebbar durch fortgesetztes manuelles Datei-Splitting, **ohne** Build-Step. Oberer Solo-Entwickler-Bereich. **≈ 8.4/10.**

*Zwei Erst-Fassungs-Fehler, in v999 korrigiert (beide aus „verify-before-citing"-Verstößen): (1) CAM als offene Lücke geführt — existiert seit v59. (2) BUILD-SPIKE als Top-Hebel — die Cold-Load-Begründung war ein HTTP/1.1-Mentalmodell, die Produktion läuft auf HTTP/2 (gemessen). Beide Empfehlungen zurückgezogen. Lehre: Architektur-/Performance-Annahmen vor dem Empfehlen am realen Code/Host messen, nicht aus Backlog/Modell ableiten.*

---

## Priorisierte Empfehlungen (Review)

> **Zwei Korrekturen (v999):**
> **(a) CAM** — die ursprüngliche Empfehlung #1 „CAM realisieren" war ein Review-Fehler. CAM existiert seit sw **v59** (browser-verifiziert); ich hatte den veralteten ROADMAP-Backlog übernommen, ohne den Code zu prüfen („verify-before-citing"-Falle). Restlücke Schnellzugriff in v999 geschlossen.
> **(b) BUILD-SPIKE verworfen** — die Cold-Load-Begründung war ein **HTTP/1.1-Mentalmodell auf einer HTTP/2-Produktion**. `curl -I` gegen GitHub Pages bestätigt **HTTP/2 + gzip + ETag**: alle 57 JS-Dateien werden über *eine* Verbindung gemultiplext, gemessener Bundle-Gewinn = wenige ms (für User nicht wahrnehmbar; Boot-Zeit dominiert vom GEDCOM-Parse, nicht vom Script-Laden). Zweite Verwechslung: Bundling löst das God-Module **gar nicht** — das war nie eine Bundler-Frage. → Architektur-Hebel ist stattdessen **GEDCOM-SPLIT** (manuelles `<script>`-Splitten, kein Build-Step).

| Prio | Maßnahme | Begründung | Aufwand |
|---|---|---|---|
| ~~—~~ | ~~CAM~~ | ✅ erledigt (Basis v59 + Schnellzugriff v999) | — |
| ~~—~~ | ~~BUILD-SPIKE~~ | ❌ verworfen — HTTP/2 gemessen, kein User-Nutzen; löst God-Module ohnehin nicht | — |
| **1** | **SCALE-REAL** — realer 50k+-Bestand | die 20k sind synthetisch; letzter unbewiesener Stabilitäts-Claim | M |
| **2** | **DOC-SCREENS** — echte Screenshots statt Mockups | belegt „einsteigerfreundlich" (Kernziel) und hebt Design/UX-Note | M |
| **3** | **ONEDRIVE-AUTO** — Sync ohne manuellen Trigger | klarste reale Lücke ggü. MFT/Ancestry | L |
| **4** | **GEDCOM-SPLIT** — `gedcom.js` (2.339 Z./96 Fn) manuell entflechten | God-Module per `<script>`-Splitting reduzieren, kein Bundler — ADR-001/002 intakt | M |
| **5** | **T0-EXTRAPLACES-CLEANUP** — Restschuld der Orts-Konsolidierung | extraPlaces seit v854 read-only; Migrations-Helfer + localStorage-Keys aufräumen | S–M |
