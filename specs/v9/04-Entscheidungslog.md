# 04 — Entscheidungslog (v9-ADRs)

> Schicht: Meta · Pflege via Skill `decision-log`

Chronologisches Log der tragenden Architektur- und Produktentscheidungen des v9-Neuaufsatzes. **Unabhängig** von den v8-ADRs (die in `legacy-v8/ARCHITECTURE.md` stehen). Ein Eintrag wird angelegt, sobald eine Entscheidung fällt, die den Bau bindet oder eine Alternative bewusst verwirft.

**Status-Legende:** ✅ akzeptiert · 🟡 offen/vorläufig · ⛔ verworfen · ♻️ ersetzt durch spätere Entscheidung.

**Eintragsformat:** Kontext (warum die Frage aufkam) · Entscheidung · Konsequenz · verworfene Alternativen · Refs.

---

## ADR-v9-01 — Spezifikationsgetriebener Neuaufsatz ✅ · 2026-07-04
- **Kontext:** v8 evolutionär gewachsen, zunehmende Inkonsistenzen/Altlasten.
- **Entscheidung:** Neuaufsatz auf Basis eines **modularen Spec-Sets** (`specs/v9/`); v8-Code eingefroren als Referenz + Verhaltens-Orakel.
- **Konsequenz:** Arbeit ist spec-first; die Spec gewinnt bei Widerspruch zum Code.
- **Refs:** [00](00-Index.md), [03 Altlasten](03-Altlasten.md).

## ADR-v9-02 — Zielarchitektur „Ansatz C" (Hybrid) ✅ · 2026-07-04
- **Kontext:** Vanilla vs. Framework. v8-Schmerz = manuelle Zustand↔Ansicht-Synchronisation; zugleich starke imperative SVG-Diagramme.
- **Entscheidung:** **framework-freier, DOM-freier Domänenkern** + **reaktive UI-Schale** + **imperative Diagramm-Inseln**. Abhängigkeiten nur nach unten (INV-ARCH-1); Kern build-frei testbar (INV-ARCH-2).
- **Konsequenz:** Reaktivität nur in der Schale; Kern rein/testbar; Diagramme als Inseln.
- **Verworfen:** reines Vanilla (A — trifft den Schmerz nicht), durchgängiges Framework (B — Overhead bei Diagrammen + Build-Zwang für alles).
- **Refs:** [02](02-Zielarchitektur-v9.md).

## ADR-v9-03 — ADR-001/002 (kein Build-Step) für v9 aufgehoben ✅ · 2026-07-04
- **Kontext:** v8 verbot bewusst Build-Step/Toolchain („edit-anywhere").
- **Entscheidung:** Für v9 **nicht mehr bindend**. Build-Step für die UI-Schale erlaubt; der **Kern bleibt build-frei ausführbar** (Tests ohne Bundler).
- **Konsequenz:** ESM/TypeScript/Vite für die UI möglich; Kern-Sicherheitsnetz unabhängig von der UI-Toolchain.
- **Refs:** [02 §1/§4](02-Zielarchitektur-v9.md).

## ADR-v9-04 — Dateihandling radikal vereinfacht ✅ · 2026-07-04
- **Kontext:** v8-Dateihandling (~5 Module, OneDrive Graph API, OAuth, ETag, filemap) war der komplexeste, am wenigsten essenzielle Teil.
- **Entscheidung:** **eine Arbeitskopie (IDB)** + **ein Export-Rohr (N Serializer)** + **FileService mit 2 Save-Tiers** als einzige Plattform-Verzweigung. **Geräte-Sync macht das OS** (Sync-Ordner), nicht die App.
- **Konsequenz:** ~5 Module → ein `FileService` + Kern-Serializer; LP-2 bleibt.
- **Verworfen:** app-verwaltete Cloud als Pflicht (nur noch optionaler Adapter hinter derselben Schnittstelle).
- **Refs:** [14](14-Dateihandling.md), [03 §9](03-Altlasten.md).

## ADR-v9-05 — Navigations-Rollenmodell + form-faktor-eigenständiges UI ✅ · 2026-07-04
- **Kontext:** v8-Navigation inkonsistent (6 Slots für ~11 Ziele, versteckte Sub-Modi, Desktop = Mobile + Spalten).
- **Entscheidung:** drei Rollen (Entitäten / Ansichten-Lenses / Arbeitsflächen). **Mobile:** Bottom-Nav `Baum · Personen · Suche · Aufgaben · Mehr` + Entitäts-Segment. **Desktop:** beschriftete Sidebar + Multi-Pane + Command-Palette (⌘K). Ein einheitlicher Lens-Umschalter.
- **Konsequenz:** ein kanonischer Weg je Ziel; Diagramm-Toggle-Glyphen entfallen.
- **Refs:** [21](21-UI-UX.md), [03 §10](03-Altlasten.md).

## ADR-v9-06 — Testframework Vitest-basiert, Kontrakt-Matrix ✅ · 2026-07-04
- **Kontext:** Tests als ausführbares Spec-Orakel; v8-JXA-Fallen vermeiden.
- **Entscheidung:** **Vitest** (Node, headless, build-frei) + fast-check + Testing-Library/happy-dom + optional Playwright. **Kontrakt-Matrix** bindet jede Invariante an eine Testart (TST-2). Fixtures = v8-Echtdateien als Regressions-Orakel.
- **Konsequenz:** JXA-Fallen entfallen; Pre-Commit = schneller Kern-Subset, CI = alle Ebenen.
- **Refs:** [32](32-Testframework.md).

## ADR-v9-07 — Entwicklungsumgebung: Claude-Desktop-App als Ein-Fenster ✅ · 2026-07-04
- **Kontext:** Nutzer dirigiert den Agenten, codet nicht selbst, will **eine** Umgebung für alle Rollen.
- **Entscheidung:** **Claude-Desktop-App** (Code-Tab: Chat/Diff/Preview/Terminal/File/Tasks) als Ein-Fenster-Umgebung + **GitHub** (Quelle/Review/CI) + **GitHub Actions** (Test/Build/Deploy). Liest `CLAUDE.md` + `.claude/skills/` automatisch (verifiziert).
- **Konsequenz:** keine separate IDE nötig; Repo lokal, nicht iCloud (INV-DEV-1).
- **Verworfen:** VS Code als Cockpit (Editor-Wert ungenutzt); **Cursor als vierte Säule** (redundanter zweiter KI-Agent, zweites Regelsystem → Drift statt Konsistenz; nur sinnvoll, wenn selbst gecodet wird).
- **Refs:** [31](31-Dev-Umgebung.md).

## ADR-v9-08 — Repo außerhalb von iCloud ✅ · 2026-07-04
- **Kontext:** v8 lag in iCloud Drive → File-I/O-Falle; Build-Projekte + `node_modules` vertragen sich nicht mit Datei-Sync-Diensten.
- **Entscheidung:** **INV-DEV-1** — v9-Repo lokal (`~/dev/stammbaum-v9`), nur GitHub als „Wolke".
- **Refs:** [31 §1](31-Dev-Umgebung.md).

## ADR-v9-09 — UI-Framework: Svelte 5 + Vite ✅ · 2026-07-04
- **Kontext:** Ansatz C braucht ein reaktives Schale-Framework mit leichter Escape-Hatch ins Imperative.
- **Entscheidung:** **Svelte 5 + Vite** (Komponenten-Bibliothek, **kein** SvelteKit — statischer Build, kein Server). Runes (`$state`/`$derived`) für die Schale; `bind:this`/`$effect` als Ausstieg in die imperativen SVG-Inseln.
- **Konsequenz:** UI-Test-Tooling = `@testing-library/svelte` + happy-dom ([32 §3](32-Testframework.md)); Vite-Build → dist/ nach GitHub Pages ([31](31-Dev-Umgebung.md)). Node.js als einmalige Voraussetzung.
- **Verworfen:** Solid (kleineres Ökosystem, JSX dichter für Review), Lit (gröbere Reaktivität → näher am manuellen v8-Schmerz), React (schwerer + Reconciler-Reibung mit imperativem SVG). Setup/Kosten waren kein Unterscheidungsmerkmal (alle gratis, alle nur Node).
- **Refs:** [02 §6](02-Zielarchitektur-v9.md).

## ADR-v9-10 — INV-ARCH-1-Gate: eigenes Node-Skript statt eslint-plugin-boundaries ✅ · 2026-07-04
- **Kontext:** Baubeginn, Projekt-Skelett in `~/dev/stammbaum-v9` angelegt ([31 §2](31-Dev-Umgebung.md)). INV-ARCH-1 (Kern importiert nichts von oben, keine DOM-/Plattform-API im Kern) braucht ein CI-Gate; [32 §3](32-Testframework.md) nennt beispielhaft `eslint-plugin-boundaries`/`dependency-cruiser`.
- **Entscheidung:** Gate als schlankes eigenes Skript `tests/arch-boundary/check-arch-boundary.mjs` (regex-basiert: verbotene Imports aus `../services|../ui|../app|svelte|vite`, verbotene Globals `window`/`document`/`fetch`/`indexedDB`/`localStorage`/`navigator`), verdrahtet als `npm run check:arch`.
- **Konsequenz:** Keine zusätzliche ESLint-Flat-Config-Ebene für Boundary-Regeln beim Skelett-Aufbau; das Skript deckt exakt die zwei verbotenen Muster ab, läuft ohne zusätzliche Abhängigkeit, ist in CI (`ci.yml`) vor `lint`/`test` verdrahtet.
- **Verworfen:** `eslint-plugin-boundaries`/`dependency-cruiser` — mehr Konfigurationsfläche als der Zweck erfordert (Vereinfachen vor Erfinden). Bleibt Option, falls das Regex-Skript an Grenzfällen scheitert (dynamische Imports, Re-Exports) — dann Wechsel + neuer ADR-Eintrag.
- **Refs:** [02 §2](02-Zielarchitektur-v9.md), [32 §3](32-Testframework.md).

## ADR-v9-11 — Deterministische, injizierte ID-Vergabe im Domänenkern ✅ · 2026-07-04
- **Kontext:** Kern-Baubeginn `core/model` (Spec 10). Neue Entitäten brauchen GEDCOM-konforme IDs (`@Ixx@`/`@Fxx@`/`@Sxx@`/`@Rxx@`/`@Nxx@`). Eine ID-Quelle im Kern darf weder Zufall noch Wall-Clock lesen (TST-3, INV-ARCH-1).
- **Entscheidung:** Fortlaufender Zähler je Präfix (I/F/S/R/N), gekapselt in einem `IdAllocator`. `nextId(alloc, prefix)` vergibt fortlaufend; `allocatorFromDatabase(db)` leitet den kollisionsfreien Startzustand aus vorhandenen IDs ab (höchste belegte Nummer je Präfix + 1). Der Allocator wird von außen gehalten/injiziert, nicht global im Kern erzeugt.
- **Konsequenz:** ID-Sequenzen sind test-reproduzierbar (gleicher Startzustand → gleiche Sequenz); die einzige Nichtdeterminismus-Quelle (Startzustand) ist explizit und injizierbar. Deckt sich mit dem injizierten Takt für `CHAN/DATE` ([32 §5](32-Testframework.md)).
- **Verworfen:** `crypto.randomUUID`/`Math.random` (verletzt TST-3 — Zufall im Kern) und Wall-Clock-basierte IDs (verletzt INV-ARCH-1 — Plattform-/Uhr-Referenz im Kern). Nicht-GEDCOM-Handles bleiben Roundtrip-Fidelity-Feldern vorbehalten (z. B. `grampsHandle`), sind aber keine Primär-IDs.
- **Refs:** [10 §1](10-Domaenenmodell.md), [32 §5](32-Testframework.md), [02 §2/§4](02-Zielarchitektur-v9.md).

## ADR-v9-12 — Eine autoritative QUAY-Ableitung im Kern (`evalToQuay`) ✅ · 2026-07-04
- **Kontext:** Kern-Baubeginn `core/research` (Spec 12). Spec 12 §3 definiert die verbindliche QUAY-*Vorschlagsregel* aus dem 3-Achsen-Evidenzmodell (`original+primary`→3, `negative`→0, `authored/undetermined/indirect`→1, sonst 2). Das Skelett hatte in `core/model/citation.ts` bereits ein zweites, score-basiertes `suggestQuayFromEval` mit abweichenden Werten (z. B. `authored`→0 statt →1) — zwei divergierende QUAY-Regeln in einem Kern.
- **Entscheidung:** Genau **eine** autoritative Regel: `evalToQuay(ev)` in `core/research/eval.ts` (Spec 12 §3, geordnete Regel — erste Übereinstimmung gewinnt: negative → original+primary → 1-Klasse → 2). `suggestQuayFromEval` (Spec 10, INV-C2) delegiert an `evalToQuay`. Beide bleiben rein informativ und setzen QUAY nie automatisch (INV-C2). Die Forschungstypen (`ResearchTask`/`LogEntry`/`Hypothesis`) ersetzen die `unknown[]`-Platzhalter an `Person`/`Family`; `EvidenceEval` erhält das optionale `informant`-Feld (`_INFM`, Spec 12 §3). Alle Nichtdeterminismus-Quellen (`created`/`date`) werden injiziert (TST-3).
- **Konsequenz:** Keine Doppel-Wahrheit bei der QUAY-Ableitung; die Regel lebt an einer Stelle (Spec-12-Territorium), das Modell verweist darauf. Zitat-QUAY (Quellqualität) und Hypothesen-`weight` (Forscher-Konfidenz) bleiben strukturell getrennt (INV-H1); Hypothesen-Evidenz ist reine SID-Referenz ohne Zitatkörper (INV-H2). Modell→Forschung-Import folgt dem Abhängigkeitspfeil 10→12.
- **Verworfen:** Zwei parallele QUAY-Heuristiken (score-basiert + Spec-Regel) nebeneinander stehen lassen — bewusst verworfen (Doppel-Wahrheit, Drift-Risiko; „Vereinfachen vor Erfinden"). Ein eigener Zitatkörper an der Hypothese (Zwei-Schichten-Evidenz / Alternativ-Baum) — verworfen, bräche Roundtrip-Treue (Spec 12 §4, INV-H2).
- **Refs:** [12 §3/§4](12-Forschungsdaten.md), [10 §5.3](10-Domaenenmodell.md), [32 §6](32-Testframework.md).

## ADR-v9-13 — Orts-/Hof-Auflösung als zustandslose reine Funktion mit Bootstrap-Rückgabe ✅ · 2026-07-04
- **Kontext:** Kern-Baubeginn `core/places` (Spec 11). Die v8-Auflösung (`_link`/`_placeLink`/`_tryHof*`) war stateful: sie mutierte `AppState.db.hofObjects` direkt und hing an lazy-gecachten Registries (`UIState._placeRegistry`/`_hofRegistry`). Spec 11 §4.1 verlangt jedoch eine **reine, totale, deterministische Funktion** `(events, placeObjects, hofObjects) → (placeId, hofId, …)` als Property-Test-Naht. Der Konflikt: die Bootstrap-Pfade (C, B') **erzeugen** Höfe — das ist ein Seiteneffekt.
- **Entscheidung:** `resolveEvents(events, places, hofObjects)` gibt eine **Arbeitskopie** der `hofObjects` (inkl. gebootstrappter Höfe) und die aufgelösten Event-Kopien als Ergebnis zurück, statt die Eingabe zu mutieren. Registries (`makePlaceRegistry`/`makeHofRegistry`) sind reine `context → registry`-Funktionen ohne Zustand. `findOrCreateHof` gibt einen neu zu erzeugenden `HofObject` als `created` zurück (mutiert die Sammlung nicht selbst). INV-PLACE (Reprojektion) ist in `resolveOne` strukturell verankert: **jeder** Rückgabepfad läuft durch denselben `reproject()`-Wrapper — es gibt keinen Pfad ohne Reprojektion, Stale-Cache ist ausgeschlossen. Die vier Chokepoints (`eventPlaceId`/`eventHofId`/`eventCoords`/`buildPlacForGedcom`) sind die einzige Lese-Naht.
- **Konsequenz:** Die Auflösung ist direkt property-testbar (gleiche Eingabe → gleiche Ausgabe, fast-check über 300 Läufe grün), reihenfolgestabil und idempotent (Re-Auflösung des Ergebnisses legt keine neuen Höfe an). Der Aufrufer (UI-Schale / Load-Pfad, Spec 14/21) übernimmt die zurückgegebene `hofObjects`-Sammlung und persistiert sie (`orte.json`, Spec 30) — die Persistenz-Entscheidung liegt außerhalb des reinen Kerns. Die *Algorithmen* aus v8 (Konvention-α-Extract, Read-Tolerance, `resolveAddrAsOf`, deterministischer HofId-Slug) sind wiederverwendet; die **drei-Speicher-/Cache-Struktur** wurde bewusst nicht übernommen (Altlast). Verwaltungs-Match (PLAC=Dorf) und Hof-Link (ADDR=Hof) sind orthogonal: Schritt 3 verankert das Dorf, ohne zurückzukehren, wenn eine explizite `ADDR` vorliegt — der Hof läuft danach über Pfad B/B' (spiegelt v8 `_placeLink` ohne `return`, dann `_tryHofAddrLink`).
- **Verworfen:** In-Place-Mutation der `hofObjects`-Map wie in v8 (verletzt die Reinheit / Property-Naht, Spec 11 §4.1). Persistenz der Auflösungs-Ergebnisse (`placeId`/`hofId`) als Sidecar/Custom-Tags — verworfen: Re-Derivation *ist* die Persistenz (LP-5, Spec 11 §4.1), `event.placeId`/`hofId` bleiben runtime-only. Per-Event-Override-Annotation für unauflösbare Fälle — verworfen (widerspräche dem Determinismus, Spec 11 §6); Ungewissheit bleibt dauerhaft im Review (Klassen A/C/D).
- **Refs:** [11 §3/§4/§5/§6](11-Orte-Hoefe-Identitaet.md), [32 §5/§6](32-Testframework.md), [02 §2](02-Zielarchitektur-v9.md).

## ADR-v9-14 — Interop-Roundtrip über einen struktur-erhaltenden Passthrough-Baum (Fidelity-Backbone) ✅ · 2026-07-04
- **Kontext:** Kern-Baubeginn `core/interop` (Spec 13). Kernversprechen ist der byte-treue Roundtrip: `MeineDaten_ancestris.ged` (83k Z., 2795 INDI-Records) → `net_delta=0` + `out1===out2`; `Unsere Familie.gramps` (5,7 MB XML) → `xml1===xml2`. Die naheliegende, aber falsche Route wäre ein *modell-rekonstruktiver* Writer (jedes Feld aus dem Domänenmodell an kanonischer Position neu erzeugen) — der driftet unvermeidlich byte-weise von der Quelle ab (Feld-Reihenfolge, unmodellierte Tags, Formatnuancen) und reproduziert die 10 v8-Ad-hoc-Passthrough-Kontexte als neue Altlast ([03 §4](03-Altlasten.md)).
- **Entscheidung:** Der Parser baut einen **generischen Zeilenbaum** `{level, xref, tag, value, children[]}` (GEDCOM, `gedcom-tree.ts`) bzw. einen **struktur-erhaltenden XML-Baum** (GRAMPS, `xml-tree.ts`). Dieser Baum ist die **einzige Quelle der Roundtrip-Treue** (INV-PT); das Domänenmodell ([10](10-Domaenenmodell.md)) ist eine **Projektion** aus dem Baum, die nur dem Editieren dient. Der Writer serialisiert bei nicht-mutierendem Speichern **primär den Baum** — der HEAD ist die einzige kontrollierte Ausnahme (verbatim aus `header.raw`, `1 DATE`/`2 TIME` nur über die injizierte Clock bei echtem Speichern, TST-3). GED7-/Strict-Export sind reine Baum-Adapter (`transformGed7`/`stripStrict`); Anonymisierung ist ein reiner Baum-Filter mit injiziertem Bezugsjahr. Der GRAMPS-Test-Seam (`parseXMLText`/`buildXMLText`, [13 §6](13-Interop-Roundtrip.md)) ist synchron und gzip-/Blob-frei (die Plattform-Randschicht legt gzip später darum).
- **Konsequenz:** `net_delta=0` und `out1===out2`/`xml1===xml2` entstehen **strukturell** — die geparste Struktur wird beim nicht-mutierenden Roundtrip exakt wiedergegeben (für die GEDCOM-Orakel-Datei: logische Zeilen der Ausgabe == logische Zeilen der Quelle, Position für Position). Ein **einziger** INV-PT-Mechanismus ersetzt die zehn v8-Kontexte; jeder unbekannte Tag/jedes unbekannte Element überlebt in Reihenfolge und Tiefe, ohne eigene Sonderbehandlung. Modellierte `_`-Tags werden nicht doppelt geschrieben, weil der Baum die einzige Schreibquelle ist (keine zweite kanonische Ausgabestelle). Kein Datenverlust bei GRAMPS trotz Whitespace-Normierung (Element-/Attribut-/Textparität geprüft; Ziel ist Writer-Idempotenz, nicht Byte-Gleichheit zur Quelle — Register `DEV-03`).
- **Verworfen:** Modell-rekonstruktiver Writer als Primärpfad (Byte-Drift, Altlast-Reproduktion) — bewusst verworfen. Ein echter DOM/`DOMParser` für GRAMPS (verletzt INV-ARCH-1: DOM-frei im Kern) — verworfen zugunsten eines dependency-freien String-XML-Parsers. **Offen (bewusst, App-Schritt):** das Zurückprojizieren *editierter* Modellfelder an ihre kanonische Baumposition ([13 §2.1](13-Interop-Roundtrip.md), 2. Halbsatz) — das ist ein Editier-Pfad, kein Roundtrip-Pfad; bis dahin ist der Kern-Roundtrip vollständig verlustfrei.
- **Refs:** [13 §2/§3/§4/§5/§6/§7](13-Interop-Roundtrip.md), [10 §5](10-Domaenenmodell.md), [11 §5](11-Orte-Hoefe-Identitaet.md), [32 §4/§5/§6/§9](32-Testframework.md), [02 §2](02-Zielarchitektur-v9.md).

## ADR-v9-15 — FileService: fünf schmale Adapter-Interfaces statt eines Plattform-Blocks; kein `fake-indexeddb` ✅ · 2026-07-04
- **Kontext:** Baubeginn `services/file` (Spec 14). Die Tier-Auswahl-Logik (INV-FILE-3) und die Arbeitskopie-Verwaltung (INV-FILE-1) müssen headless mit gemockten Adaptern testbar sein (Spec 32 §5), ohne dass Tests je eine echte Plattform-API (IndexedDB, File System Access API, `navigator.share`, `<a download>`) berühren.
- **Entscheidung:** `services/file/types.ts` definiert fünf schmale Interfaces (`WorkingCopyStore`, `PickerAdapter`, `FsHandleAdapter`, `ShareAdapter`, `DownloadAdapter`), die `FileService` injiziert bekommt (`FileServiceAdapters`-Bündel). `FileService` selbst (`file-service.ts`) enthält ausschließlich Orchestrierung/Tier-Auswahl, keine Plattform-Referenz. Tests mocken die Interfaces mit einfachen In-Memory-Fakes (`tests/services/mock-adapters.ts`) — **kein** `fake-indexeddb` als Dev-Dependency, weil die zu prüfende Logik (Tier-Wahl, Arbeitskopie-Ersetzung) nicht von echter IndexedDB-Transaktionssemantik abhängt; ein `load/save/clear`-Interface genügt (Vereinfachen vor Erfinden). Die reale IndexedDB-Implementierung (`idb-working-copy-store.ts`) nutzt einen Object-Store mit festem Key `"current"` — erzwingt INV-FILE-1 strukturell, nicht nur per Konvention. Das Export-Rohr (`export-pipe.ts`) führt alle vier Formate (`gedcom-5.5.1`/`gedcom-strict`/`gedcom-7.0`/`gramps`) durch dieselbe `FileService.exportToFile()` (INV-FILE-2); die GRAMPS-Gzip-Hülle hängt selbst hinter einem injizierbaren `GzipAdapter`, damit `core/interop` nie `CompressionStream`/DOM berührt.
- **Konsequenz:** INV-FILE-1/2/3 sind je durch benannte Tests mit gemockten Adaptern verriegelt (4+6+7 = 18 Tests, `tests/services/file-service.test.ts` + `export-pipe.test.ts`); keine Test-Assertion ruft eine ungemockte Plattform-API auf. Neue Tiers (z. B. ein optionaler Cloud-Adapter, Spec 14 §5) lassen sich als sechstes schmales Interface ergänzen, ohne bestehende Adapter-Verträge zu ändern.
- **Verworfen:** `fake-indexeddb`-Dev-Dependency (unnötige Komplexität für das, was tatsächlich geprüft werden muss). Ein einziges großes `PlatformAdapter`-Interface statt fünf schmaler — verworfen, weil es die Tier-Auswahl-Logik unnötig koppeln und gezieltes Mocken erschweren würde.
- **Refs:** [14 §3/§4](14-Dateihandling.md), [32 §5/§6](32-Testframework.md), [02 §7](02-Zielarchitektur-v9.md).

## ADR-v9-16 — ViewState als Svelte-5-Runes-Factory (kein Modul-Singleton) + zwei Tooling-Fixes für Svelte-5-Komponenten-Tooling ✅ · 2026-07-04
- **Kontext:** Erste UI-Scheibe der reaktiven Schale (Spec 21 §5, INV-VS): Personen-Liste/-Detail, Bottom-Nav, Import-Einstieg. INV-VS verlangt *eine* zentrale Auswahl-Instanz je Ziel — ohne dabei die aus v8 bekannte Falle (verstreute `currentX`/`_lastTabSel`) durch einen neuen globalen Modul-Zustand zu ersetzen, der Komponenten-Tests koppeln würde. Beim ersten Bauversuch scheiterten `npm run lint` und die Component-Tests an zwei unabhängigen Tooling-Lücken zwischen Svelte 5 + TypeScript-ESLint + Vitest.
- **Entscheidung:** (1) `createViewState()` in `ui/shell/view-state.svelte.ts` ist eine **Factory**, die `$state`/kein Modul-Top-Level hält; `setCurrent(target,id)`/`getCurrent(target)` + ein einfacher `subscribe`-Kanal für nicht-Svelte-Konsumenten bilden den einzigen Schreib-/Lesepfad. Persistenz über App-Resume ist bewusst ausgeklammert (Folge-Schritt, im Code kommentiert). Analog `createAppState()` für die geladene Datenbank + den daraus abgeleiteten `PlaceContext` (Chokepoints, Spec 11 §5). (2) `eslint.config.js` bekommt einen expliziten `languageOptions.parserOptions.parser = tseslint.parser`-Block für `**/*.svelte`/`**/*.svelte.ts` — `eslint-plugin-svelte` `flat/recommended` setzt zwar korrekt `svelte-eslint-parser`, delegiert das eingebettete `<script>` aber ohne diesen Zusatz an `espree`, das an jeder TS-Syntax (Interfaces, Generics, `type`-Imports) scheitert. (3) `vitest.config.ts` bekommt `resolve.conditions:['browser']`; ohne das löst Vite in Component-Tests den Svelte-Server-Build auf und `mount()` wirft `lifecycle_function_unavailable`. Vitest 4 hat `environmentMatchGlobs` entfernt — die Environment-Wahl pro Testdatei bleibt der Docblock `// @vitest-environment happy-dom`, kombiniert mit dem globalen `resolve.conditions`.
- **Konsequenz:** Jeder Test (Kern wie Komponente) erzeugt seine eigene `ViewState`/`AppState`-Instanz — kein Test kann über einen geteilten Modul-Zustand einen anderen beeinflussen. Die zwei Tooling-Fixes gelten pauschal für den gesamten weiteren UI-Bau (jede künftige `.svelte`/`.svelte.ts`-Datei profitiert automatisch, kein Pro-Datei-Sonderfall nötig). `npm run lint`/`npm test`/`npm run build` sind mit dieser Konfiguration grün (225 Tests, davon 31 neu für die erste UI-Scheibe: INV-VS-Vertrag, Personen-Listen-/Detail-Modell, Quellen-Badge, 3 Component-Tests mit `@testing-library/svelte` + happy-dom).
- **Verworfen:** Svelte-Stores (`writable`) für ViewState — nicht der in ADR-v9-09 entschiedene Runes-Ansatz. Ein Modul-Singleton (`export const viewState = createViewState()`) — verletzt Testisolation, genau die v8-Falle (verstreuter, implizit geteilter Zustand), die INV-VS auflösen soll.
- **Refs:** [21 §5](21-UI-UX.md), [02 §3/§6](02-Zielarchitektur-v9.md), [32 §3/§6](32-Testframework.md), ADR-v9-09.

## ADR-v9-17 — Entitäten-Tab-Umbrella (`EntityTab.svelte`) statt Pro-Tab-Segment-Switcher; zentrale Cross-Entitäts-Navigation ✅ · 2026-07-04
- **Kontext:** Zweite UI-Scheibe (Spec 20 §1.5 Familien-Tab, §1.6 Quellen-Tab & Archive). Die erste Scheibe hatte den Entitäts-Segment-Umschalter als reinen Platzhalter innerhalb von `PersonTab.svelte` angelegt (nur „Personen" befüllt, andere Segmente deaktiviert). Mit Familien und Quellen kommen zwei weitere funktionale Segmente hinzu, plus Cross-Entitäts-Navigation (Familie→Person, Person→Familie, Person/Familie→Quelle, Quelle→Archiv→Quelle), die laut Spec 21 §1 INV-UI-2 („genau ein kanonischer Weg") und INV-VS („eine Auswahl-Instanz je Ziel") zentral verwaltet werden muss, nicht verstreut in jeder Detail-Komponente.
- **Entscheidung:** `PersonTab.svelte` wird durch `ui/views/EntityTab.svelte` ersetzt — eine Umbrella-Komponente, die den Segment-Umschalter (Personen/Familien/Quellen/Orte[folgt]/Höfe[folgt]) UND die Cross-Entitäts-Navigation zentral hält. Navigation zwischen Entitäten läuft über benannte Callbacks (`onNavigateToPerson`/`onNavigateToFamily`/`onNavigateToSource`/`onNavigateToRepository`), die `EntityTab` an die einzelnen Detail-Komponenten (`PersonDetail`/`FamilyDetail`/`SourceDetail`/`RepositoryDetail`) durchreicht. Jeder Navigations-Callback wechselt in `EntityTab` sowohl den aktiven Segment-State als auch die ViewState-Auswahl des Zielsegments in einem Zug — ein Aufruf, kein zweiter, getrennter Schreibpfad. Archive (Repository) sind kein eigener Top-Level-Segment-Button (Spec 20 §1.6 gruppiert „Quellen-Tab & Archive" als eine Einheit), sondern ein Sub-View innerhalb des Quellen-Segments (`sourceSubView: 'sources'|'repositories'`), erreichbar über einen Archiv-Link in der Quellen-Detailseite oder einen eigenen Archiv-Reiter. Der initiale Segment/Sub-View beim Mount leitet sich einmalig (per `untrack`) aus einer bereits vorhandenen ViewState-Auswahl ab (z. B. nach App-Resume oder wenn ein Aufrufer vor dem Mount schon eine Auswahl gesetzt hat) — aber ohne fortlaufendes Re-Sync bei jeder späteren ViewState-Änderung (sonst würde z. B. ein Zurück-Klick in der Personenliste den Nutzer aus einem bewusst gewählten Segment werfen). Zusätzlich bekommt `SourceBadge.svelte` einen optionalen `onSelect`-Callback-Prop — mit Callback rendert sie einen `<button>` (Navigation zur Quellen-Detailseite), ohne Callback bleibt sie eine reine `<span>`-Anzeige. `ViewTarget` wächst um `'family'`/`'source'`/`'repository'`; `BottomNav` bekommt einen eigenen, engeren `BottomNavTarget`-Typ (nur die 5 tatsächlichen Bottom-Nav-Ziele).
- **Konsequenz:** Ein einziger Ort (`EntityTab.svelte`) kennt alle Cross-Entitäts-Sprünge und hält Segment-Wahl + ViewState synchron — verhindert die v8-Falle verstreuter Ad-hoc-Navigationslogik in einzelnen Detail-Views. Neue Entitäten-Segmente (Orte/Höfe, künftiger Durchgang) docken am selben Muster an. 33 neue Model-Unit-Tests + 26 neue Component-Tests (davon 8 in `EntityTab.component.test.ts`, die den Segment-Wechsel + jede Cross-Navigation-Route abdecken) — Gesamt 284/284 grün.
- **Verworfen:** (a) Jede Detail-Komponente ruft `viewState.setCurrent()` für fremde Ziele direkt selbst auf, ohne zentrale Segment-Koordination — verworfen, weil dann Segment-State und ViewState-Auswahl auseinanderlaufen könnten (z. B. Quellen-Segment aktiv, aber ViewState zeigt auf eine Person). (b) Archive als eigener Top-Level-Segment-Button gleichrangig zu Personen/Familien/Quellen — verworfen, widerspricht Spec 20 §1.6 und hätte INV-UI-2 verletzt (zwei Wege zu Quellen-nahen Daten). (c) Fortlaufendes Re-Sync von `activeSegment` aus ViewState bei jeder Änderung (statt einmalig beim Mount) — verworfen, würde Nutzer unerwartet aus einem manuell gewählten Segment werfen.
- **Refs:** [20 §1.5/§1.6](20-Funktionen.md), [21 §1/§2/§5](21-UI-UX.md), ADR-v9-16.

## ADR-v9-18 — Eine Nachname-Extraktionsfunktion (`surnameCandidate`) für Personen- UND Familien-Sortierung ✅ · 2026-07-04
- **Kontext:** Dritte UI-Scheibe (Sortierung/Suche/Filter für Personen- und Familien-Tab, [20 §1.4/§1.5](20-Funktionen.md)). Bei der Browser-Verifikation (mit mehreren Personen, deren Vorname- und Nachname-Alphabet bewusst gegenläufig liefen — z. B. „Otto Anders"/„Carl Mueller") fielen **zwei zusammenhängende, aber unabhängig entstandene Bugs** auf, die kein bestehender Test fing, weil alle bisherigen Test-Fixtures Vorname/Nachname-Alphabet zufällig parallel liefen: **(1)** Der Personen-Sortier-Modus „Name" sortierte über `displayName(p)` (Format „Vorname Nachname") statt über den Nachnamen — der Buchstaben-Trenner (nach Nachname gruppiert) und die tatsächliche Zeilen-Reihenfolge liefen auseinander. **(2)** Die Familien-Sortierung nach „Nachname Ehemann/Ehefrau" las `person.surname` direkt — das Feld ist bei `core/interop/gedcom-parse.ts` NUR gesetzt, wenn die GEDCOM-Quelle explizite `GIVN`/`SURN`-Untertags hatte; bei der verbreiteten reinen Form `1 NAME Otto /Anders/` blieb es leer, wodurch die Sortierung still auf Vergleich der vollen Anzeigezeile zurückfiel (faktisch Sortierung nach Vorname statt Nachname).
- **Entscheidung:** `surnameCandidate(p)` (`ui/shell/person-display.ts`, vormals eine private Hilfsfunktion nur für `sortLetter`) wird zur **einzigen, exportierten** Quelle für „was ist der Nachname einer Person" — mit Fallback-Kette `p.surname → p.name.split('/')[1] → p.given → p.name`. Ein neuer `sortKey(p)` (Format „Nachname Vorname", nicht „Vorname Nachname" wie `displayName`) nutzt denselben Kandidaten für den Personen-Sortier-Modus „Name". `family-list-model.ts`s bis dahin eigene, fallback-lose `surnameFor()` wurde entfernt und ruft jetzt `surnameCandidate()` auf. `displayName()` bleibt unverändert (reine Anzeige-Reihenfolge „Vorname Nachname" ist bewusst korrekt für die Leseform, nur ungeeignet als Sortierschlüssel).
- **Konsequenz:** Nur noch **eine** Stelle im Code entscheidet „was zählt als Nachname" — jede künftige Sortierung/Gruppierung nach Nachname (Personen, Familien, später ggf. Orte/Höfe-Bezüge) muss `surnameCandidate()` wiederverwenden, nicht neu erfinden. Zwei Regressionstests verriegeln beide Bugs mit bewusst gegenläufigen Vorname/Nachname-Testdaten (`tests/ui/person-list-model.test.ts`, `tests/ui/family-list-model.test.ts`) — die alten Tests mit parallel laufendem Alphabet bleiben als (schwächere) Positivfälle bestehen. **Lehre für künftige Test-Fixtures:** Wo eine Sortier-/Gruppierlogik getestet wird, IMMER mindestens einen Fall mit divergierenden Sortierschlüsseln einbauen (hier: Vorname- vs. Nachname-Alphabet) — sonst kann eine falsche Sortier-Spalte unentdeckt bleiben, weil zufällig parallele Testdaten beide Hypothesen gleichermaßen erfüllen.
- **Verworfen:** Jede Sortier-Stelle behält ihre eigene Nachname-Ermittlung (Status quo vor diesem ADR) — verworfen, das ist exakt die Ursache beider Bugs (Drift zwischen unabhängigen Implementierungen derselben Frage).
- **Refs:** [20 §1.4/§1.5](20-Funktionen.md), [32 §6](32-Testframework.md) (Kontrakt-Zeile 20: „jede Validierungsregel hat einen Test").

## ADR-v9-19 — `linkEventToPlace()` reprojiziert `ev.place` sofort im Kommando (Sofort-Reprojektion) ✅ · 2026-07-04
- **Kontext:** Vierte UI-Scheibe, Orte-/Höfe-Tab ([20 §1.7/§1.8](20-Funktionen.md)). Die „String→PlaceObject verknüpfen"-Aktion im Orte-Tab führte ein neues Kern-Kommando ein, `linkEventToPlace(ev, placeId)` (`core/places/commands.ts`), das ausschließlich `ev.placeId = placeId` setzt. INV-PLACE ([11 §3](11-Orte-Hoefe-Identitaet.md)) verlangt jedoch: sobald `event.placeId`/`event.hofId` gesetzt ist, ist `event.place` **ausschließlich** die zwischengespeicherte Projektion `buildPlacForGedcom(event, year)`, und Anzeige/Export leiten **sofort** aus dem Modell ab — „es gibt keinen Pfad ohne Reprojektion". Der interne Auflösungscode (`core/places/resolve.ts`, `resolveOne()`) hält dieses Versprechen strukturell ein: ein `reproject()`-Wrapper läuft am Ende **jedes** Pfads (A/A'/B/B'/C/hierarchy-*/atomic-po/reproject). `linkEventToPlace()` ist aber ein neuer, externer UI-Mutationspfad außerhalb von `resolveOne()` und durchläuft diesen Wrapper nicht — `ev.place` bleibt nach der Aktion die alte Roh-Zeichenkette, bis irgendwann erneut aufgelöst wird. Verschärfend kommt hinzu: `resolveEvents()` wird aktuell an **keiner** Stelle der laufenden App aufgerufen (noch nicht in den Lade-/Import-Pfad verdrahtet — separater Befund, Gegenstand von orte.json-Loader-Arbeit), es existiert also derzeit auch kein „nächster Load-Zyklus", der den Stale-Cache nachträglich heilen würde.
- **Entscheidung:** Der Befund wird hier festgehalten, **ohne** ihn in diesem Zug zu beheben. Ziel-Entscheidung für die spätere Korrektur: `linkEventToPlace()` (oder jeder künftige UI-Mutationspfad, der `placeId`/`hofId` direkt setzt) muss `ev.place` unmittelbar per `buildPlacForGedcom(ev, year, ctx)` reprojizieren — kein UI-Kommando darf INV-PLACE umgehen, unabhängig davon, ob/wann die App `resolveEvents()` beim Laden verdrahtet.
- **Konsequenz:** Bis zur Korrektur zeigt die UI nach „verknüpfen" ggf. eine veraltete `ev.place`-Zeichenkette, obwohl `ev.placeId` bereits korrekt gesetzt ist — ein bekannter, dokumentierter Zustand, kein stiller Datenverlust (die Wahrheit `ev.placeId` ist korrekt, nur die Projektions-Anzeige hinkt hinterher). Jede künftige Korrektur an `linkEventToPlace()` (oder Einführung weiterer direkter `placeId`/`hofId`-Setter) muss diesen ADR-Eintrag auf ✅ setzen und auf einen Regressionstest verweisen, der die Sofort-Reprojektion verriegelt (analog `tests/core/inv-place-reprojection.test.ts`).
- **Verworfen:** Stillschweigendes Belassen ohne Dokumentation — verworfen (widerspricht „Roundtrip-/Invarianten-Abweichungen nie stillschweigend übertünchen"). Sofortige Korrektur im selben Zug wie der Orte-Tab-Bau — bewusst zurückgestellt (Scope-Entscheidung dieser Session), um die UI-Scheibe nicht mit einer Kern-Korrektur zu vermischen, deren Testarbeit (Property-Test-Erweiterung) eigenständig ist.
- **Refs:** [11 §3](11-Orte-Hoefe-Identitaet.md), [20 §1.7](20-Funktionen.md), [32 §6](32-Testframework.md).
- **Update 2026-07-04 (nach ADR-v9-20):** Der fünfte Bauabschnitt (`services/places`, ADR-v9-20) verdrahtet `resolveEvents()` erstmals in den laufenden Ladepfad (GEDCOM-Import). Die Lücke ist damit nicht mehr nur theoretisch — jeder Import löst jetzt tatsächlich eine Auflösung aus, die `linkEventToPlace()` weiterhin umgeht (die Lücke betrifft ausschließlich den UI-Kommando-Pfad „verknüpfen", nicht den Import-Pfad selbst). Priorität für die Korrektur steigt entsprechend; Status bleibt 🟡, bis behoben.
- **Update 2026-07-05 (Audit-Befund — Prämisse selbst strittig):** Dieser ADR setzt als „Ziel-Entscheidung" die **Sofort-Reprojektion** voraus (Lesart a). Der aktuelle Code-Kommentar in `core/places/commands.ts` vertritt jedoch die **Gegenposition** (Lesart b): Nicht-Reprojizieren sei **Absicht**, Reprojektion gehöre ausschließlich in den Load-Pfad ([11 §4.1](11-Orte-Hoefe-Identitaet.md) „Re-Derivation ist die Persistenz"). Damit ist nicht nur die *Umsetzung* offen, sondern schon die *Richtung*. Die Frage ist als offene Spezifikationsfrage in [11 §8 Punkt 4](11-Orte-Hoefe-Identitaet.md) verankert; deren Entscheidung schließt diesen ADR (→ ✅ mit Regressionstest bei Lesart a, → ⛔/♻️ bei Lesart b). Bis dahin bleibt 🟡, aber „Korrektur zurückgestellt" ist präziser als „Richtung noch nicht entschieden".
- **Update 2026-07-05 (entschieden → ✅ Sofort-Reprojektion):** Richtung ratifiziert (Nutzer-Entscheidung): **Lesart (a) / Variante (ii) — Sofort-Reprojektion im Kommando**. `linkEventToPlace` (und jeder künftige direkte `placeId`/`hofId`-Setter) reprojiziert `event.place` unmittelbar per `buildPlacForGedcom(ev, year, ctx)` — als reine Kern-Funktion INV-ARCH-1-konform. [11 §3](11-Orte-Hoefe-Identitaet.md) (INV-PLACE) bleibt wörtlich hart und wird um den Zwei-Zeitpunkt-Mechanismus präzisiert, [11 §4.1](11-Orte-Hoefe-Identitaet.md) hält fest: Reprojektion läuft bei **jedem Laden** UND in **jedem Modell-Kommando**; persistiert wird weiterhin nur `placeId`/`hofId`, nie `event.place`. [11 §8.4](11-Orte-Hoefe-Identitaet.md) ist damit geschlossen. Status **✅ (akzeptiert)**; der Code-Nachtrag in `core/places/commands.ts` (Lesart-b-Kommentar korrigieren + Sofort-Reprojektion) samt Regressionstest (analog `tests/core/inv-place-reprojection.test.ts`) ist Phase 1.2.
- **Umsetzung 2026-07-05 (Phase 1.2, ✅ erledigt):** Code-Repo `~/dev/stammbaum-v9`, Commit `248b399`. `linkEventToPlace(ev, placeId, ctx)` setzt `placeId` UND reprojiziert `ev.place` sofort via `buildPlacForGedcom` (Signatur um `PlaceContext` erweitert; `PlaceDetail.svelte` reicht `appState.placeContext`). Der Lesart-(b)-Kommentar in `commands.ts` ist korrigiert. Regressionstest in `tests/core/places-commands.test.ts` verriegelt die Sofort-Reprojektion (periodengerechte pname + null-Guard bei unbekannter placeId). 768 Tests / lint / tsc / arch grün.

## ADR-v9-20 — `services/places`: orte.json-Browser-Spiegel + `resolveEvents()`-Wiring im Import-Pfad; Sync-Ordner-Dateipfad zurückgestellt ✅ · 2026-07-04
- **Kontext:** Fünfter Bauabschnitt, direkt im Anschluss an den Orte-/Höfe-Tab (ADR-v9-17ff.). Befund: `db.placeObjects`/`db.hofObjects` wurden nach GEDCOM-Import nie befüllt — `ui/shell/ImportButton.svelte` rief nur `parseGedcom()` und `appState.loadDatabase()`, nie `resolveEvents()` aus `core/places` (Spec 11 §4) und nie einen Loader für `orte.json` (Spec 14 §6, Spec 11 §2: `placeObjects`/`hofObjects` sind **cross-stammbaum**-Wissen, nicht Teil der Genealogie-Datei). Ohne beides blieben Orte-/Höfe-Tab nach echtem Import leer bzw. die Chokepoints (`eventPlaceId`/`eventHofId`) hatten nichts zum Matchen, und Hof-Bootstrap aus Adress-Mustern (nur innerhalb `resolveEvents()` möglich) fand nie statt.
- **Entscheidung:** Neues Modul `services/places/` (Bruder von `services/file/`, gleiches Adapter-Muster wie ADR-v9-15: `PlacesStore`-Interface, injizierte `Clock`/`DeviceIdProvider` für Determinismus in Tests analog ADR-v9-11). `orte.json` wird für diese Slice **nur als Browser-Spiegel** (IndexedDB) gebaut — der in Spec 14 §6/Spec 30 §2.1 vorgesehene **Sync-Ordner-Dateipfad** (OS synct die Datei neben der Genealogie-Datei) ist bewusst **zurückgestellt**, folgt aber später über denselben `PlacesStore`-Vertrag (kein neuer Adapter-Typ nötig). Wiring: `ImportButton.svelte` lädt nach `parseGedcom()` den `orte.json`-Spiegel, sammelt alle Events aus `db.persons`/`db.families` (`birth/chr/death/buri/events[]` bzw. `marriage/engagement/events[]`), ruft `resolveEvents()` einmal auf, schreibt die aufgelösten Event-Kopien an ihre Originalstelle zurück, übernimmt das (ggf. durch Bootstrap gewachsene) `hofObjects`-Ergebnis, speichert bei Änderung zurück — **erst danach** läuft `appState.loadDatabase()`.
- **Konsequenz:** Orte-/Höfe-Tab sind nach echtem GEDCOM-Import jetzt tatsächlich befüllt (browser-verifiziert: `PLAC Wall 33, Ochtrup, Kreis Steinfurt` + `ADDR Wall 33` löst nach Vorab-Seed eines `Ochtrup`-PlaceObject korrekt zu Hof „Wall 33" im Dorf „Ochtrup" auf). `core/places` blieb unverändert (INV-ARCH-1) — `services/places` ruft ausschließlich dessen bestehende öffentliche API auf. GRAMPS-Import-Wiring ist nicht Teil dieser Slice (nur GEDCOM-Pfad existiert aktuell). Die Union-Merge-/Konflikt-Feinheiten sind eigenständig in ADR-v9-21 festgehalten; der dabei gefundene IndexedDB-Schema-Konflikt in ADR-v9-22.
- **Verworfen:** `orte.json` sofort als volle Sync-Ordner-Datei (mit Directory-Handle-Auflösung wie bei Medien, [14 §7](14-Dateihandling.md)) umzusetzen — zurückgestellt, um diese Slice nicht mit Datei-System-Zugriffsrechten (FS-Access-Permission-Flows) zu vermischen; der Browser-Spiegel liefert bereits den vollen Cross-Stammbaum-Nutzen für ein einzelnes Gerät. GRAMPS-Import gleich mitverdrahten — verworfen, außerhalb des beauftragten Scopes dieser Slice.
- **Refs:** [14 §6](14-Dateihandling.md), [11 §2/§4](11-Orte-Hoefe-Identitaet.md), [30 §2.1/§4](30-NFR-und-Persistenz.md), ADR-v9-15, ADR-v9-11.

## ADR-v9-21 — Union-Merge-Härtung: auch bei fortgeschrittener Revision (nicht nur „anderes Device“) ✅ · 2026-07-04
- **Kontext:** Teil von ADR-v9-20 (`services/places`, `reconcileAndSave()`). Der Spec-Wortlaut (Spec 30 §4, Spec 11 §2) beschreibt Union-Merge exakt für den Fall „gleiche Revision + verschiedenes Device + abweichender Inhalt". Beim Bau fiel ein Lücken-Fall auf, den der Wortlaut nicht abdeckt: die lokale Fassung baut auf einer `baseRev` auf (aus einem vorherigen `loadPlaces()`); wenn der gespeicherte Stand zwischenzeitlich bereits eine **höhere** `rev` hat als `baseRev` — unabhängig davon, ob dieselbe oder eine andere Geräte-ID geschrieben hat (z. B. dasselbe Gerät hat zwischenzeitlich in einem zweiten Tab/Prozess gespeichert) —, wäre ein reines „schreibe drüber" ein stilles Last-Write-Wins, genau das, was LP-9 (Multi-Device-Konfliktschutz) verhindern soll.
- **Entscheidung:** `reconcileAndSave()` löst Union-Merge in ZWEI Fällen aus, nicht nur einem: **(a)** `remote.rev === baseRev && remote.device !== eigene deviceId` (Spec-Wortlaut) **oder** **(b)** `remote.rev > baseRev` (der gespeicherte Stand ist bereits weiter als der, auf dem lokal aufgebaut wurde — unabhängig vom Device). Beide Fälle laufen über dieselbe Merge-Politik: alle IDs aus beiden Seiten bleiben erhalten (Union), bei gleicher ID mit abweichendem Inhalt gewinnt die Seite mit dem neueren Wrapper-Zeitstempel `ts` — bewusst kein Feld-Level-Merge (Überkonstruktion für den erreichten Funktionsstand).
- **Konsequenz:** Kein Szenario führt mehr zu stillem Last-Write-Wins, auch nicht Same-Device-Mehrfachschreiben (z. B. zwei Tabs). `sameContent()` (strukturelle Gleichheit über die JSON-Wire-Form) verhindert unnötige Warnungen, wenn beide Seiten ohnehin identisch sind — nur echte Inhalts-Kollisionen erzeugen die `union-merge`-Warnung mit den betroffenen IDs.
- **Verworfen:** Strikt nur der Spec-Wortlaut (Fall a) — verworfen, weil er den Same-Device-Mehrfachschreiben-Fall stillschweigend LWW ließe, was dem Sinn von LP-9 widerspricht.
- **Refs:** [30 §4](30-NFR-und-Persistenz.md), [11 §2](11-Orte-Hoefe-Identitaet.md), ADR-v9-20.

## ADR-v9-22 — Ein zentraler IndexedDB-Schema-Öffner (`services/idb-schema.ts`) statt Store-lokalem `indexedDB.open()` ✅ · 2026-07-04
- **Kontext:** Browser-Verifikation von ADR-v9-20 deckte einen Bug auf: `idb-working-copy-store.ts` (`services/file`, ADR-v9-15) und das neue `idb-places-store.ts` (`services/places`) öffneten je unabhängig `indexedDB.open('stammbaum-v9', 1)` mit je einem eigenen `onupgradeneeded`-Handler, der nur den eigenen Object-Store anlegte. `indexedDB.open()` feuert `onupgradeneeded` aber nur EINMAL pro Versionssprung, unabhängig davon, wie viele Module `open()` aufrufen — beim ersten echten Öffnen gewann nur der zuerst aufgerufene Handler, der zweite Object-Store existierte nie. Folge: `IDBDatabase.transaction` warf „One of the specified object stores was not found" beim ersten GEDCOM-Import.
- **Entscheidung:** `services/idb-schema.ts` als EIN gemeinsamer Öffner für alle `services/*`-IndexedDB-Stores: `openStammbaumDb()` mit `DB_VERSION=2`, ein einziger `onupgradeneeded`-Handler, der ALLE bekannten Object-Stores anlegt (`working-copy`, `places-mirror`). `idb-working-copy-store.ts` und `idb-places-store.ts` rufen jetzt beide `openStammbaumDb()` statt eigenes `indexedDB.open()`. Ein Regressionstest (`tests/services/idb-schema.test.ts`) verriegelt strukturell, dass kein anderes Modul unter `services/` direkt `indexedDB.open()` aufruft.
- **Konsequenz:** Jedes künftige `services/*`-Store-Modul MUSS seinen Object-Store in `services/idb-schema.ts` ergänzen (Analog zum „ein Export-Rohr"-Prinzip, INV-FILE-2, hier für Storage-Setup) statt einen eigenen `indexedDB.open()`-Aufruf zu bauen — sonst wiederholt sich exakt dieser Bug bei jedem weiteren Store. `services/file` (ADR-v9-15) ist rückwirkend auf den zentralen Öffner umgestellt; sein Adapter-Vertrag (`WorkingCopyStore`) bleibt unverändert, nur die interne `indexedDB.open()`-Stelle wandert.
- **Verworfen:** Je Modul ein eigener `indexedDB.open()` mit höherer Store-spezifischer DB-Version (z. B. `stammbaum-v9-places`) — verworfen, weil das zwei separate IndexedDB-Datenbanken erzeugt hätte (funktioniert zwar, aber unnötige Verdopplung ohne Vorteil; „Vereinfachen vor Erfinden").
- **Refs:** [14 §3.1](14-Dateihandling.md), [11 §2](11-Orte-Hoefe-Identitaet.md), ADR-v9-15, ADR-v9-20.

## ADR-v9-23 — Geschwisterzeile ist [K]-Bestandteil der Sanduhr, nicht [S]/[E]; in der ersten Sanduhr-Slice versehentlich ausgelassen ✅ · 2026-07-04
- **Kontext:** Sechster Bauabschnitt, Sanduhr-Baum als erste imperative Diagrammm-Insel (`ui/islands/tree/`, Spec 20 §1.3, Spec 02 §5). Der Bauauftrag dieser Slice grenzte explizit auf die zum Zeitpunkt in Spec 20 §1.3 gelisteten [K]-Punkte ab („Sanduhr-Baum: … + Proband + Ehepartner + Kinder. Kekule-Nummern. Mehrfach-Ehen, Halbgeschwister"). Der `legacy-v8`-Code (`ui-views-tree.js`, Verhaltens-Orakel) hat die Sanduhr aber immer MIT einer eigenen Geschwisterzeile des Probanden gebaut — die Spec-Zeile nannte nur die `½`-Halbgeschwister-**Markierung** (als Flag auf fremden Kindern in Mehrfach-Ehen-Konstellationen), nicht die Geschwisterzeile als eigenes, strukturelles Baumelement. Dadurch wurde die Geschwisterzeile in der ersten Sanduhr-Slice bewusst-aber-fälschlich als „nicht [K], separater Bauabschnitt" eingestuft und ausgelassen (dokumentiert als offener Punkt im Abschlussbericht dieser Slice).
- **Entscheidung:** Die Geschwisterzeile GEHÖRT zum [K]-Umfang der Sanduhr, exakt wie im v8-Orakel — sie ist kein optionaler Zusatz, sondern Teil des erreichten v8-Funktionsstands, den Spec 20 laut Präambel („Referenz-Katalog des erreichten v8-Umfangs") verbindlich abbilden soll. `specs/v9/20-Funktionen.md` §1.3 wird entsprechend präzisiert (neue [K]-Zeile „Geschwisterzeile des Probanden"). **Umsetzung ist NICHT Teil dieses Eintrags** — der Code-Nachtrag (Geschwisterzeile in `ui/islands/tree/tree-model.ts`/`tree-layout.ts`/`hourglass-tree.ts` ergänzen: Voll-/Halbgeschwister aus den Kindern der Eltern-Familie(n) außer dem Proband selbst ermitteln, als eigene Zeile neben/um den Proband positionieren, Halbgeschwister weiterhin mit `½` markieren) ist als eigener Folge-Bauabschnitt eingeplant.
- **Konsequenz:** Spec-Korrektur zuerst (dieser Eintrag + `20-Funktionen.md`), Code-Nachtrag folgt als eigene, klar abgegrenzte Slice (test-first: reine Traversal-Erweiterung in `tree-model.ts` unit-testbar, Layout-Erweiterung in `tree-layout.ts`, Browser-Verifikation der Geschwisterzeile im Sanduhr-Rendering). Bis zur Umsetzung zeigt die Sanduhr KEINE Geschwister des Probanden — ein bekannter, dokumentierter Rückstand ggü. dem v8-Funktionsstand, kein stiller Verlust.
- **Umgesetzt in zwei Commits** (`~/dev/stammbaum-v9`, v9-dev): **`888d086`** — Geschwisterzeile selbst (Voll-/Halbgeschwister aus `person.childOf`, `½`-Markierung, T-Junction-Connector). Bei der Verifikation dieses ersten Commits fiel eine zweite, im ursprünglichen Auftrag nicht mitgegebene v8-Verhaltenseigenschaft auf: die Zeile war unbegrenzt breit (`sibRowW = nSibs·W + …`), bei z. B. 10 Geschwistern schob das `personCX` um >1000px und Desktop-Auto-Fit skalierte die GANZE Sanduhr winzig klein. **`8662604`** behebt das durch 1:1-Portierung des v8-Overflow-Verhaltens (`ui-views-tree.js` Z. 391-392/476-524/667-700): horizontale Zeile nur bei ≥3 sichtbaren Ahnen-Ebenen mit dynamisch schrumpfender Kartenbreite (Minimum 52/60px) + „…"-Overflow-Indikator (Tooltip „+N Geschwister nicht dargestellt") wenn nicht alle passen; bei <3 Ahnen-Ebenen ein Peek-Stapel-Fallback, der ALLE Geschwister zeigt (kein Cutoff) + Zähler-Badge am Proband. Browser-verifiziert in beiden Modi (10-Geschwister-Fixtures, Landscape 3-Ebenen vs. Portrait/2-Ebenen-Fallback). 520 → 526 Tests grün. **Durable Lehre:** beim Portieren eines v8-Layout-Algorithmus als „Orakel" reicht es nicht, den Grundfall (wenige Elemente) zu übernehmen — die Kapazitäts-/Overflow-Behandlung (v8 löst „zu viele Elemente in einer Zeile" praktisch überall über Schrumpfen+Cutoff+Peek-Fallback) ist Teil desselben Verhaltens und muss explizit mit beauftragt werden, sonst bleibt sie unbemerkt bis zum ersten Datensatz mit vielen Geschwistern/Kindern.
- **Verworfen:** Die Auslassung stillschweigend als „bewusste Vereinfachung" stehen lassen (wie in den anderen offenen Punkten dieser Slice, z. B. Namens-Kürzung) — verworfen, weil die Geschwisterzeile anders als jene Punkte ein bestehendes v8-[K]-Feature ist, keine neue Design-Frage; das rechtfertigt eine Spec-Korrektur statt nur eine Notiz.
- **Refs:** [20 §1.3](20-Funktionen.md), [02 §5](02-Zielarchitektur-v9.md), `legacy-v8/ui-views-tree.js`.

## ADR-v9-24 — Höfe gehören in die globale Suche; Spec-Aufzählung war unvollständig ✅ · 2026-07-04
- **Kontext:** Siebter Bauabschnitt, globale Suche (`ui/views/search/`, Spec 20 §1.1). Der Spec-Text nannte wörtlich „Globale Suche … über Personen/Familien/Quellen/Orte" — ohne Höfe. Die Bau-Slice hielt sich absichtlich wortgetreu daran (auf explizite Anweisung, Spec-Abweichungen nicht selbst zu entscheiden) und flaggte die Auslassung als offenen Punkt, strukturell identisch zum bereits in ADR-v9-23 behobenen Geschwisterzeilen-Fund: Höfe sind im Orte-/Höfe-Tab ([20 §1.8](20-Funktionen.md)) ein gleichrangiges Entitäten-Segment neben Personen/Familien/Quellen/Orte (`EntityTab.svelte` behandelt `place` und `hof` als gleichwertige Segmente) — es gibt keinen inhaltlichen Grund, warum Höfe von „finde irgendwas" (Spec 21 §2) ausgenommen sein sollten.
- **Entscheidung:** Höfe werden in die globale Suche aufgenommen. `specs/v9/20-Funktionen.md` §1.1 präzisiert (Aufzählung um „Höfe" ergänzt). Umsetzung: fünfte Ergebnisgruppe „Höfe" in `ui/views/search/global-search-model.ts`/`GlobalSearchView.svelte`, unter Wiederverwendung der bereits vorhandenen `matchesSearch`-Logik aus `ui/views/hof/hof-list-model.ts` (analog zum bisherigen Muster: Personen-/Familien-/Orte-Matcher wurden bereits exportiert und wiederverwendet statt dupliziert, ADR-v9-18-Lehre). Klick auf einen Hof-Treffer navigiert in das Höfe-Segment von `EntityTab` (`viewState.setCurrent('hof', id)`), analog zum bestehenden Orte-Navigationspfad.
- **Konsequenz:** Alle sechs Entitäten-Segmente (Personen/Familien/Quellen/Orte/Höfe — Archive bleiben Sub-View der Quellen, s. ADR-v9-17) sind jetzt konsistent sowohl über den Entitäten-Segment-Umschalter als auch über die globale Suche erreichbar; kein Segment ist mehr „zweiter Klasse". **Umgesetzt in Commit `07b2e90`** (`~/dev/stammbaum-v9`, v9-dev): `hof-list-model.ts`s `matchesSearch()`/`toRow()` exportiert (Wiederverwendung statt Dupliziert, wie geplant), `global-search-model.ts` um `hofs`-Gruppe erweitert, `GlobalSearchView.svelte` + `App.svelte` verdrahtet. 503 → 507 Tests grün, browser-verifiziert (Suche über Adresse UND Dorf-Titel, Klick navigiert ins Höfe-Segment).
- **Verworfen:** Höfe dauerhaft aus der globalen Suche ausgenommen lassen (Status quo) — verworfen, kein tragfähiger inhaltlicher Grund gefunden, nur eine Auslassung in der ursprünglichen Spec-Formulierung.
- **Refs:** [20 §1.1/§1.8](20-Funktionen.md), [21 §2](21-UI-UX.md), ADR-v9-17, ADR-v9-18, ADR-v9-23.

## ADR-v9-25 — Karten-Lens: Leaflet + OSM-Tiles als Primärpfad, kachel-freier SVG-Weltumriss als Offline-Fallback ✅ · 2026-07-05
- **Kontext:** Neunter Bauabschnitt, Karten-Lens (`ui/islands/`, Spec 20 §1.9 [S], imperative Insel). Der v8-Orakel-Code (`legacy-v8/ui-views-map.js`) baut die Karte mit **Leaflet 1.9.4** (lokal gebündelt) + `L.tileLayer` gegen OpenStreetMap-Kacheln, 3 Modi (Orte/Personen-Cluster/Migrationen), Zoom/Pan/Animation, und einem Offline-Banner (`_updateMapOfflineBanner`) statt einer Blockade, wenn keine Kacheln geladen werden können. Für v9 ist das eine echte Architekturfrage, keine reine Umsetzungsfrage: `package.json` hat aktuell **keine einzige `dependencies`-Eintragung** — nur `devDependencies` (Svelte kompiliert weg, alles andere ist Tooling). Leaflet wäre die **erste echte Laufzeit-Drittanbieter-Bibliothek** im gesamten v9-Bau (anders als `core/places`/`core/model`/`ui/islands/tree` — durchgängig framework-frei). Zusätzlich steht das gegen LP-2 (Lokal-First)/Offline-PWA-Anspruch (Spec 20 §1.2 [K] Offline-Betrieb): Kachel-Bilder brauchen Internet gegen OpenStreetMap-Server, das ist ein bewusster, von v8 bereits akzeptierter Kompromiss (Offline-Banner statt Blockade), keine neue Erfindung. Nutzer wurde explizit zwischen drei Optionen befragt (1: Leaflet wie v8; 2: nur eine selbst gebaute, kachel-freie SVG-Weltumriss-Karte, konsequent offline-fähig aber ohne echten Detailgrad; 1+2 hybrid) — Entscheidung: **Hybrid** („1 mit 2 als offline fallback").
- **Entscheidung:** Karten-Lens nutzt **Leaflet + OpenStreetMap-Tiles als Primärpfad** (deckt alle 3 Modi aus Spec 20 §1.9 aus dem v8-Orakel ab: Orte/Personen-Cluster/Migrationen-Animation), **ergänzt um einen selbst gebauten, kachel-freien SVG-Weltumriss als Offline-Fallback** (grobe Projektion ohne echte Straßenkarten/Zoom-Detailgrad, aber funktioniert ohne Netzwerk — löst dieselben 3 Modi auf einer vereinfachten Projektionsbasis, konsequent LP-2 für den Offline-Fall). Damit übernimmt v9 den v8-Funktionsstand vollständig (kein Rückschritt gegenüber dem erreichten Umfang, Spec-20-Präambel), mildert aber die Netzwerkabhängigkeit ab, die v8 nicht abgemildert hatte (dort nur ein Banner, keine funktionierende Offline-Alternative).
- **Konsequenz:** `package.json` bekommt erstmals einen `dependencies`-Eintrag (`leaflet`); der Bauabschnitt muss zusätzlich entscheiden/dokumentieren, wie der Umschaltpunkt zwischen Leaflet-Pfad und SVG-Fallback erkannt wird (Netzwerk-/Tile-Ladefehler-Erkennung) — das ist Umsetzungsdetail des Bauabschnitts selbst, kein weiterer ADR-Bedarf, solange kein grundsätzlich anderer Ansatz gewählt wird. Die beiden Modi teilen sich nach Möglichkeit dieselbe Datenaufbereitung (Orts-/Personen-/Migrations-Koordinaten aus `core/places`-Chokepoints), nur die Rendering-Schicht unterscheidet sich.
- **Verworfen:** Nur Leaflet ohne Fallback (wie v8, 1:1) — verworfen, weil das den LP-2-Kompromiss unvermindert übernimmt, obwohl v9 ausdrücklich die Gelegenheit hat, ihn abzumildern. Nur eine selbst gebaute SVG-Karte ohne Leaflet — verworfen, weil das den erreichten v8-Funktionsstand (echte Weltkarte mit Straßendetail, Zoom, Kachel-Panning) unterschreiten würde, was Spec 20 laut Präambel („Referenz-Katalog des erreichten v8-Umfangs") vermeiden soll.
- **Umgesetzt in Commit `65f5c19`** (`~/dev/stammbaum-v9`, v9-dev): `ui/islands/map/` mit `leaflet-map.ts` (Primärpfad, `leaflet` als erster `dependencies`-Eintrag), `svg-fallback-map.ts` (kachel-freier Weltumriss) und `map-model.ts` (gemeinsame Datenaufbereitung über `core/places`-Chokepoints für alle 3 Modi). Umschaltpunkt = `navigator.onLine` + Leaflet-`tileerror`-Event (das in der Konsequenz offen gelassene Umsetzungsdetail). Browser-verifiziert inkl. erzwungenem Offline-Fallback.
- **Refs:** [20 §1.9](20-Funktionen.md), [02 §5](02-Zielarchitektur-v9.md) (imperative Inseln), [30](30-NFR-und-Persistenz.md) (Offline/PWA), `legacy-v8/ui-views-map.js`.

## ADR-v9-26 — INV-UI-4: wiederkehrende visuelle Muster haben genau eine Quelle; Lehre in Spec + Agenten-Dateien verankert ✅ · 2026-07-05
- **Kontext:** Nutzer entdeckte per Screenshot-Vergleich **zweimal hintereinander** dasselbe strukturelle Problem im selben Bauabschnitt (Lens-Umschalter + Karten-Lens, ADR-v9-20/-25 Umfeld): **(1)** `TreeView.svelte` und `MapLensView.svelte` hatten je eine eigene, unabhängig duplizierte `__topbar`-Kopfzeile mit redundantem Titel-Text („Baum"/„Karte") ÜBER dem `LensSwitcher`, der dieselbe Information über das aktive Tab bereits zeigte — unterschiedliche Höhe je nachdem, ob eine View einen Kontext-Button (Vollbild) hatte oder nicht. **(2)** `EntityTab.svelte`s Entitäten-Segmente und `LensSwitcher.svelte` implementierten unabhängig voneinander zwei sichtbar verschiedene Segment-Control-Stile (individuell umrandete Gold-Pillen vs. gemeinsamer Track mit transparenten Items + `flex:1`-Zeilenumbruch-Bug bei längeren Labels). Beide Funde wurden noch in derselben Session behoben (Commits `9662d18`/`0893dd8`, neue gemeinsame Komponenten `LensViewHeader.svelte` bzw. gemeinsame Klassen `.stb-segment-row`/`.stb-segment-btn` in `design-system.css`) — aber der zugrunde liegende Denkfehler (jede View erfindet ihr eigenes „sieht aus wie ein Tab/eine Kopfzeile"-CSS neu, statt zuerst nach einer bestehenden Lösung zu suchen) war in beiden Fällen identisch und wiederholte sich, obwohl der erste Fund bereits hätte warnen sollen. Nutzer-Auftrag: „stelle in Skills oder Spezifikation eine Lessons-Learnt sicher, so dass ähnliche Anzeigen auch gleich realisiert werden" — die Lehre soll **präventiv** wirken, nicht nur zwei weitere Ad-hoc-Fixes bleiben.
- **Entscheidung:** Neue Invariante **INV-UI-4** in [21 §6](21-UI-UX.md) (Design-System): wiederkehrende visuelle Muster (Segment-Control, View-Kopfzeile, Listenzeile, Badge, …) haben **genau eine** CSS-/Komponenten-Quelle; vor dem Bau eines neuen „sieht aus wie X"-Elements MUSS zuerst `design-system.css` + bestehende `ui/views`/`ui/shell`-Dateien nach einem vergleichbaren Muster durchsucht werden. Zusätzlich in **beiden** UI-bauenden Agenten-Definitionen verankert (`.claude/agents/ui-builder.md`, `.claude/agents/islands-builder.md`, „Harte Regeln"-Abschnitt) — nicht nur in der Spec, weil die Agenten die Spec zwar lesen, ein knapper, direkt am Arbeitsauftrag stehender Merksatz mit dem konkreten Beispiel (Segment-Control, Kopfzeile) aber die tatsächliche Vergiss-Wahrscheinlichkeit stärker senkt als ein Verweis auf ein weiteres Spec-Kapitel.
- **Konsequenz:** Jeder künftige UI-Bauabschnitt (ui-builder wie islands-builder) muss vor dem Schreiben von component-lokalem CSS für ein wiederkehrendes Muster explizit prüfen/kurz vermerken, ob eine bestehende `.stb-*`-Klasse oder Shell-Komponente (`LensViewHeader`, `LensSwitcher`, …) zutrifft. Verstöße sind ab jetzt ein Review-Fund mit Verweis auf INV-UI-4, kein Neuland mehr.
- **Verworfen:** Nur eine ADR-Notiz ohne Spec-/Agenten-Änderung — verworfen, das hätte exakt das Symptom belassen (Wissen liegt vergraben im Log, wird vor dem nächsten Bau nicht zwangsläufig gelesen). Eine neue, dritte Kategorie parallel zu INV-UI-1/2/3 statt Erweiterung derselben Nummerierung — verworfen zugunsten von INV-UI-4 als natürliche Fortsetzung derselben Invarianten-Familie (alle vier sind Navigations-/Darstellungs-Konsistenzregeln aus [21 §1/§4/§6](21-UI-UX.md)).
- **Refs:** [21 §6](21-UI-UX.md) (INV-UI-4, neu), `.claude/agents/ui-builder.md`, `.claude/agents/islands-builder.md`, Commits `9662d18`/`0893dd8` (~/dev/stammbaum-v9).

## ADR-v9-27 — Orte-Bootstrap aus GEDCOM-PLAC nur als Opt-in-Vorschlag, nie automatisch ♻️ (ersetzt durch ADR-v9-28) · 2026-07-05
- **Kontext:** Beim Demo-Modus-Bauabschnitt (Spec 20 §1.2, Commit `5a85809`) fiel — browser-verifiziert, nicht nur vermutet — auf: Orte-Tab, Höfe-Tab und der Karten-„Orte"-Modus bleiben nach JEDEM reinen GEDCOM-Import leer (Demo-Datensatz eingeschlossen), weil `parseGedcom` `db.placeObjects` nie befüllt und `core/places/resolve.ts`s Verwaltungs-Match-Pfade (3a/3b/3c) laut Spec 11 §4.2 ausdrücklich ein BEREITS EXISTIERENDES `placeObject` voraussetzen (`ctx.places.findByName`) — nur der Hof-Bootstrap (Pfade C/B') erzeugt neue Entitäten automatisch, der Village-/Orts-Bootstrap ist im Algorithmus schlicht nicht vorgesehen. Der Leer-Zustand-Text im Orte-Tab („werden beim Laden einer Datei automatisch gesammelt") ist dadurch irreführend. Diese Leere war beim ersten öffentlichen „Ausprobieren" der App (Demo-Modus) das erste Mal spürbar geworden, obwohl sie strukturell schon seit ADR-v9-20 (`services/places`-Wiring) bekannt und dokumentiert war.
- **Kernfrage:** Ist das ein Bug (Orte SOLLTEN automatisch wie Höfe bootstrappen) oder Absicht (`placeObjects` sind laut Spec 11 §2 eine **kuratierte** Cross-Stammbaum-Schicht, `orte.json` — automatische Erzeugung aus jedem rohen PLAC-String würde exakt den unkuratierten Orts-Wildwuchs zurückbringen, den das v8-Redesign ADR-024 beheben sollte, s. `legacy-v8/ARCHITECTURE.md`)? Nutzer wurde explizit befragt (3 Optionen: automatischer Bootstrap wie Höfe / Opt-in-Vorschlag mit Nutzer-Sichtung / nur Leer-Text korrigieren ohne neuen Mechanismus) — Entscheidung: **Opt-in-Vorschlag**.
- **Entscheidung:** Orte-Tab bekommt eine Aktion „Orte aus dieser Datei vorschlagen" (o. ä. Label) — sammelt distinkte PLAC-Hierarchien aus den geladenen Events, bietet sie als PlaceObject-**Entwürfe** zur Sichtung/Bestätigung an (kein stilles Anlegen). Erst nach Nutzer-Bestätigung werden daraus echte `placeObjects` (→ `orte.json`, dieselbe Persistenz wie jeder andere Ort). Bewahrt die Kuration (Spec 11 §2), löst aber das „leeres Tab beim ersten Import"-Problem, ohne den Verwaltungs-Match-Algorithmus selbst (Spec 11 §4.2) zu verändern — sobald Entwürfe bestätigt sind, greifen Pfade 3a/3b/3c ganz normal beim nächsten Load.
- **Konsequenz:** `specs/v9/20-Funktionen.md` §1.7 (Orte-Tab) wird um diesen Punkt ergänzt (neue [K]-Zeile, s. dort — ohne sie bleibt der Orte-/Höfe-Tab für den mit Abstand häufigsten Eingabeweg, reines GEDCOM, praktisch nutzlos, das ist keine optionale Erweiterung mehr). Umsetzung ist NICHT Teil dieses Eintrags — eigener Bauabschnitt (Kern: Vorschlags-Sammlung reine Funktion in `core/places`, analog `findOrCreateHof` aber ohne automatisches Schreiben; UI: Orte-Tab-Aktion + Sichtungs-Dialog). Der irreführende Leer-Zustand-Text im Orte-Tab wird im selben Bauabschnitt korrigiert.
- **Verworfen:** Automatischer Bootstrap wie bei Höfen — verworfen, würde die kuratierte Natur von `placeObjects` (Spec 11 §2, „Cross-Stammbaum"-Anspruch) unterlaufen und bei mehrdeutigen/varianten-reichen GEDCOM-Quellen (unterschiedliche Schreibweisen desselben Orts über mehrere Familienzweige) zu unkontrollierten Dubletten führen — exakt das v8-ADR-024-Problem. Nur den Leer-Text korrigieren ohne neuen Mechanismus — verworfen, weil das „leeres Tab bei häufigstem Eingabeweg" ungelöst ließe; das ist mehr als ein Formulierungsproblem.
- **Umgesetzt (Opt-in-Variante) in Commits `9804873` (Kern `suggestPlaceCandidates`) + `a05fd26` (Orte-Tab-Sichtungsdialog).**
- **Update 2026-07-05 — Richtung revidiert, Neuentscheidung ausstehend:** Nach Praxistest hat der Nutzer die Grundentscheidung dieses ADR („Opt-in, nie automatisch") **verworfen**: Orte sind genealogische Standardinformation und müssen nach JEDEM GEDCOM-Import **sofort** sichtbar sein (auch mit anfänglicher PLAC-Inkonsistenz, die dann per Werkzeug nachbearbeitbar ist) — anders als die Hof-Zusatzfunktion, bei der Nacharbeit akzeptabel bleibt. Der offene Auftrag ist als Spec-Restklasse in [11 §8 Punkt 3](11-Orte-Hoefe-Identitaet.md) verankert (+ Session-Task `task_a82678c1`). Bis eine Neuentscheidung als eigener ADR vorliegt und Spec 11 §2/§4.2 überarbeitet ist, bleibt die gebaute Opt-in-UI in Kraft, aber der ADR gilt richtungsseitig als überholt (wird dann auf ♻️ ersetzt gesetzt). **Konsequenz für 20 §1.7:** die dort ergänzte [K]-Zeile beschreibt noch die Opt-in-Variante — sie wird mit der Neuentscheidung mitgezogen.
- **Abschluss 2026-07-05 → ♻️ ersetzt durch ADR-v9-28:** Die Neuentscheidung liegt vor: automatischer Village-Seed beim Import (Auto-Seed statt Opt-in). Spec 11 §2/§4.2/§8 und Spec 20 §1.7 sind entsprechend überarbeitet. Der gebaute Opt-in-Sichtungsdialog (Commits `9804873`/`a05fd26`) wird in Phase 2 durch den Auto-Seed abgelöst; die Kern-Sammelfunktion `suggestPlaceCandidates` wird zur Basis von `seedPlacesFromEvents` wiederverwendet.
- **Refs:** [11 §2/§4/§8](11-Orte-Hoefe-Identitaet.md), [20 §1.7](20-Funktionen.md), ADR-v9-20, ADR-v9-28, `legacy-v8/ARCHITECTURE.md` (ADR-024).

## ADR-v9-28 — Orte werden beim Import automatisch aus PLAC geseedet (ersetzt ADR-v9-27) ✅ · 2026-07-05
- **Kontext:** ADR-v9-27 hatte `placeObjects` bewusst NIE automatisch aus PLAC erzeugt (nur Opt-in-Sichtungsdialog), um die kuratierte Cross-Stammbaum-Schicht ([11 §2](11-Orte-Hoefe-Identitaet.md)) vor unkontrollierten Dubletten zu schützen (v8-ADR-024-Problem). Praxistest (2026-07-05): aus Nutzersicht inakzeptabel — Orte sind genealogische Standardinformation (anders als die Hof-Zusatzfunktion) und müssen nach JEDEM GEDCOM-Import sofort sichtbar sein; der Opt-in-Zwang ließ Orte-/Höfe-Tab + Karten-„Orte"-Modus beim häufigsten Eingabeweg (reines GEDCOM) leer. Der Nutzer akzeptiert anfängliche PLAC-Inkonsistenz explizit, sofern sie nachgelagert per Werkzeug bereinigbar ist.
- **Entscheidung:** Ein **Village-Seed-Vorpass** (`seedPlacesFromEvents`, reine/deterministische Kern-Funktion, injizierte IDs analog ADR-v9-11) läuft beim Import VOR `resolveEvents` und erzeugt aus den distinkten PLAC-Hierarchien die fehlenden Village-`PlaceObjects` (+ datierte `enclosedBy`-Kette). `placeObjects` ist damit **auto-seeded UND kuratiert** ([11 §2](11-Orte-Hoefe-Identitaet.md) überarbeitet). Welches PLAC-Segment das Village ist, entscheidet der Seed mit **demselben Signal** wie die Konventions-Matrix ([11 §4.3](11-Orte-Hoefe-Identitaet.md)): Konvention-1-Hof-Fall (Hof-Event-Typ + `event.addr`-Extract trifft das Leitsegment) → Village = `segs[1..]`; sonst → Leitsegment. Der Seed erzeugt **nie** einen Hof — die Hof-Bootstrap-Pfade (A/A'/C/B') bleiben unverändert (Hof-Erkennung nicht verwässert). Kuration nachgelagert: Dubletten-Merge (auf **[K]** angehoben, [20 §1.7](20-Funktionen.md)) + String→PlaceObject-Verknüpfung.
- **Konsequenz:** Orte-/Höfe-Tab + Karten-Orte sind nach jedem Import befüllt, ohne Nutzeraktion. Der Verwaltungs-Match ([11 §4.2](11-Orte-Hoefe-Identitaet.md) 3a/3b/3c) bleibt algorithmisch unverändert — er findet jetzt die geseedeten POs vor (das Henne-Ei-Problem „leeres Tab" ist gelöst, ohne den Match-Algorithmus anzufassen). Schreibvarianten desselben Orts über mehrere Zweige erzeugen zunächst getrennte POs (akzeptiert) und werden über den Dubletten-Merge zusammengeführt. Der gebaute Opt-in-Sichtungsdialog (ADR-v9-27, Commits `9804873`/`a05fd26`) wird abgelöst; die Sammel-Kernfunktion `suggestPlaceCandidates` wird zur Basis von `seedPlacesFromEvents` wiederverwendet (Vereinfachen vor Erfinden). Umsetzung: Phase 1 (Kern) + Phase 2 (Lade-Pipeline/UI).
- **Verworfen:** Opt-in-Vorschlag beibehalten (ADR-v9-27) — verworfen, s. Kontext. Auto-Seed OHNE Konventions-Signal (jedes Leitsegment als Village) — verworfen: würde Konvention-1-Höfe fälschlich als PlaceObjects anlegen und die Hof-Achse verwässern. Ein Provenance-/„unbestätigt"-Pflichtfeld pro PO — verworfen für diese Slice (Vereinfachen vor Erfinden); die Kuration läuft über die bestehenden Merge-/Verknüpfungs-Werkzeuge, ein solches Flag bleibt optionale UI-Zutat.
- **Umsetzung 2026-07-05 (Phase 1.1a, Kern-Seed ✅):** `core/places/seed.ts` (`seedPlacesFromEvents`), Code-Commit `65d70c1`. Dedup-Regel s. ADR-v9-29. Noch offen: Lade-Pipeline-Wiring (Phase 2).
- **Refs:** [11 §2/§4.2/§4.3/§8](11-Orte-Hoefe-Identitaet.md), [20 §1.7](20-Funktionen.md), ADR-v9-27 (ersetzt ♻️), ADR-v9-20, ADR-v9-11, `legacy-v8/ARCHITECTURE.md` (ADR-024).

## ADR-v9-29 — Orts-Identität: Seed-Dedup nach Name+Hierarchie-Verträglichkeit, Resolver-Konsistenz-Guard (3c/3c′) und Review-Klasse P ✅ · 2026-07-05
- **Kontext:** Beim Durchdenken realer Mehrdeutigkeit rund um die Orts-Auflösung ([11 §4](11-Orte-Hoefe-Identitaet.md)) und den neuen Auto-Seed (ADR-v9-28) traten drei **gekoppelte** Lücken zutage, belegt an zwei Datensatz-Mustern:
  - **Gleichnamige Orte, unterschiedliche Verwaltungshierarchie** (`Oldenburg, Niedersachsen, Deutschland` vs. `Oldenburg, …, USA`). (a) Der Resolver-Pfad **3c** (`resolve.ts`, „Leitname eindeutig") prüft NUR globale Eindeutigkeit des Leitnamens und ignoriert die Folgesegmente völlig: existiert nur der deutsche Oldenburg als PO, wird `PLAC Oldenburg, USA` **still an den deutschen Ort gebunden** (`placeId`=NS-Oldenburg) — Falschattribution samt Reprojektion von `ev.place` auf „…, Niedersachsen, Deutschland". Existieren beide POs, bricht 3c bei Nicht-Eindeutigkeit ab → Fall **unaufgelöst**, obwohl das Elternsegment ihn eindeutig macht. (b) Atomarer `PLAC Oldenburg` trifft über `findByName` bei mehreren Kandidaten **still einen** (Typ-Rang, sonst Einfügereihenfolge) — kein Ambiguitäts-Flag; es gibt gar keine Review-Klasse für Orts-Mehrdeutigkeit (Klassen A/C/D sind hof-bezogen).
  - **Dedup-Spannung im Seed:** hunderte identische `PLAC Ochtrup` dürfen NICHT hunderte Orte werden (→ Zusammenführen nach Name), aber die zwei Oldenburgs dürfen NICHT zu einem verschmelzen (→ Trennen nach Hierarchie). Der bestehende `suggestPlaceCandidates` (`place-bootstrap.ts`) dedupliziert **name-only** (`normPlaceName(title)`) — löst Ochtrup korrekt, würde die Oldenburgs aber fälschlich kollabieren. Ein naiver Voll-Hierarchie-String-Key wiederum spaltet denselben Ort, je nachdem, ob er als `Ochtrup` oder `Ochtrup, Kreis Steinfurt` geschrieben ist.
- **Entscheidung:** Eine **einzige** Verträglichkeits-Regel trägt Seed UND Resolver:
  1. **Seed-Dedup = Name + Hierarchie-Verträglichkeit** (nicht name-only, nicht Voll-String): Kandidaten werden nach normalisiertem Leitnamen gruppiert; innerhalb einer Gruppe verschmelzen Einträge, deren Elternketten **verträglich** sind (eine ist Präfix der/konsistent mit der anderen, oder ohne Eltern) — die reichste Kette gewinnt für `enclosedBy`. Nur bei **widersprüchlichen** Eltern (auf gemeinsamer Ebene abweichend, `Niedersachsen ≠ USA`) entstehen **distinkte** POs. → hunderte `Ochtrup` = 1 PO; `Ochtrup` + `Ochtrup, Kreis Steinfurt` = 1 PO; Oldenburg/NS + Oldenburg/USA = 2 POs.
  2. **Resolver-Konsistenz-Guard (3c) + Eltern-Disambiguierung (3c′):** 3c bindet nur noch, wenn der eindeutige Leitname-Kandidat auch **hierarchie-verträglich** ist (Folgesegmente stimmen mit `enclosureChainAsOf` überein oder fehlen) — ein widersprechender Elter (`…, USA`) ist ein **Veto**, kein Match. Bei mehreren gleichnamigen Kandidaten wählt **3c′** den, dessen Kette zu den Folgesegmenten passt; genau einer → `placeId`, keiner/mehrere → Review.
  3. **Review-Klasse P (Ort mehrdeutig):** atomarer PLAC oder rich-PLAC ohne disambiguierenden Elter, der ≥2 gleichnamige POs trifft (oder dessen einziger Kandidat per Guard-Veto ausfällt), landet sichtbar im Review — analog Hof-Klasse C, nur auf der Verwaltungs-Achse. Kein stilles Raten.
- **Konsequenz:** Die drei Teile sind **gekoppelt** — nur zusammen korrekt: ohne Seed-Verträglichkeits-Key kein zweites Oldenburg-PO → 3c′ hätte nichts zu disambiguieren → Falschverknüpfung; ohne Guard bindet 3c weiter falsch; ohne Klasse P bleibt echte Mehrdeutigkeit still. Umsetzung ist **Phase 1** (test-first): 1.1a Seed-Dedup, 1.1b Guard/3c′, 1.1c Klasse P — verriegelt durch die Fixtures Ochtrup-Masse / Oldenburg-DE / Oldenburg-US / Oldenburg-atomar. `place-bootstrap.ts` (`suggestPlaceCandidates`) wird von name-only auf die Verträglichkeits-Regel gehoben; `resolve.ts` bekommt Guard+3c′; `ReviewClass` wächst um `'P'`. [11 §4.2/§6](11-Orte-Hoefe-Identitaet.md) sind entsprechend präzisiert. Echte Schreibvarianten (`Ochtrup`/`Ochtorp`) bleiben getrennt → Dubletten-Merge (ADR-v9-28), unverändert.
- **Verworfen:** **Name-only-Dedup** (Status quo `suggestPlaceCandidates`) — kollabiert Oldenburg/NS + Oldenburg/USA. **Voll-Hierarchie-String-Key** — spaltet denselben Ort nach Schreibtiefe (`Ochtrup` ≠ `Ochtrup, Kreis Steinfurt`). **3c nur über Leitname-Eindeutigkeit** (Status quo `resolve.ts`) — bindet `Oldenburg, USA` still an den deutschen Ort. **Atomare Mehrdeutigkeit still auflösen** (Typ-Rang/erster) — verwirft genealogische Information ohne Sichtbarkeit; ersetzt durch Klasse P.
- **Umsetzung 2026-07-05:** **1.1a Seed-Dedup ✅** — `core/places/seed.ts`, Commit `65d70c1`; Verträglichkeits-Regel (Name + Eltern-Präfix) verriegelt durch `tests/core/place-seed.test.ts` (hunderte Ochtrup → 1; atomar+reich → 1; Oldenburg/NS ≠ /USA → 2; existierendes Oldenburg kein Fehl-Reuse; atomar gegen ≥2 Cluster → kein Merge). **1.1b Resolver-Guard/3c′ ✅** — `core/places/resolve.ts` (`chainCompatible`), Commit `8807379`; 3c bindet nur bei verträglicher `enclosureChainAsOf`, 3c′ disambiguiert gleichnamige POs über die Eltern; verriegelt durch `tests/core/place-disambiguation.test.ts` (Guard-Veto `Oldenburg, USA`, 3c′ NS/US, lenient bare-PO, kein verträglicher Kandidat → null). **1.1c Review-Klasse P** noch offen (Rest-Mehrdeutigkeit bleibt aktuell `placeId=null`, ohne sichtbaren Review-Eintrag).
- **Refs:** [11 §4.2/§4.3/§6/§8](11-Orte-Hoefe-Identitaet.md), [20 §1.7](20-Funktionen.md), [32 §6](32-Testframework.md), ADR-v9-28, ADR-v9-13, `core/places/resolve.ts` · `place-bootstrap.ts` · `place-registry.ts`.
